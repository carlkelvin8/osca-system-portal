# OSCA System â€” Session Summary

## Objective
Build equipment borrowing with static QR workflow for Staff, attendance reports (daily/weekly/monthly) with export, facility quantity validation, a complete Audit Logs module (Admin-only), and a Venue Reservation Requests system (Coach/PE Instructor request, Admin/Director/Staff approve) â€” for ADMIN, DIRECTOR, and STAFF roles only (plus Coach/PE Instructor for reservations).

## Important Details
- Docker containers: `osca_api`, `osca_frontend`, `osca_postgres`, `osca_redis`, `osca_minio`, `osca_celery`, `osca_celery_beat`, `osca_nginx`
- RBAC: `_STAFF_BORROW_ROLES = {ADMIN, DIRECTOR, STAFF}` for new borrow endpoints
- Transaction QR format: `TXN-{id_hex[:12].upper()}` â€” auto-generated, unique, invalidated on completion
- Static QR (BorrowingID) format: `BID-{instructor_uuid}` â€” auto-generated on Coach/PE Instructor creation, permanent, never expires
- Static QR (BorrowingID) scan returns full coach/PE instructor info: eligibility, active borrows, pending requests, active sanctions
- Alembic head: `3e4f5a6b7c8d` (add announcement visibility/link_url — Publish To). Chain: `f3g4h5i6j7k8` → `9a8b7c6d5e4f` → `b1c2d3e4f5a6` → `c3d4e5f6a7b8` → `d7e8f9a0b1c2` → `1f2e3d4c5b6a` (EMAIL OTP + users.email_verified were ADDED then ROLLED BACK — migrations `a2b3c4d5e6f7`/`b2c3d4e5f6a7` deleted, table+column dropped, DB re-stamped to `1f2e3d4c5b6a`) → `2a3b4c5d6e7f` (add ip/device to attendance records) → **`3e4f5a6b7c8d`** (announcements visibility + link_url; down_revision `2a3b4c5d6e7f`)
- **Registration flow (RESTORED to pre-OTP behavior â€” email OTP REMOVED on 2026-08-11)**: Self-registration goes straight from form â†’ face registration (5 photos) â†’ Register â†’ account created with `is_active=False` (PENDING). No OTP modal, no email sending, no `/auth/registration/*` endpoints (both now 404). Login is blocked (403 "Your account is pending approval. Please wait for an administrator to activate it.") until Admin/Director/Staff activates via the users page Pending Approval tab (`PATCH /users/{id}` `{is_active:true}` â€” status update only, NO new account). Registration success page: "Your account has been created and is now pending approval." + "You will be able to sign in once an administrator activates your account." Existing `SMTP_*`/`EMAIL_FROM` env vars remain (pre-existing, unused by registration â€” kept for future email features; `.env` untouched).
- Attendance reports export via CSV (built-in `csv`), XLSX (`openpyxl`), PDF (`xhtml2pdf`)
- Facility `capacity` field: backend `ge=0` in Pydantic schema, frontend `min="0"` on number input, explicit validation message "Quantity cannot be less than 0."
- Equipment creation no longer renders/upload QR images â€” uses `EQ-{uuid_hex[:12].upper()}` as simple code
- User communicates in Taglish (Tagalog + English)
- Coach nav (ACTUAL state): coach STILL has Reports, Analytics, Facilities, Eligibility, Incidents, Sanctions + Attendance, Player Roster, Attendance Scan (`/kiosk`). The "Coach nav alignment" note below was NOT implemented â€” user chose to defer it ("iwan muna").
- Coach attendance is scoped to `assigned_sport` (coaches' `sport_or_art` is NULL â€” use `assigned_sport` in frontend/backend sport checks for coaches).
- `GET /attendance/records`: accepts `date_from`, `date_to` query params; returns `session_name`, `session_sport_or_art`; students auto-filtered to own records
- **Audit Logs = ADMIN-only** (backed up by `_EDITOR_ROLES = {UserRole.ADMIN}` in `backend/app/api/v1/audit_logs.py` + frontend nav gate). Director/Staff get 403.
- **Audit context capture**: `get_current_user`/`get_optional_user` in `core/dependencies.py` attach `user._current_request = request`; `audit_log()` service (`services/audit_service.py`) auto-falls back to `request_of(current_user)` when no explicit `request` is passed â†’ every log captures IP + browser + OS + device + URL + method.
- **Audit call sites** now use the `audit_log()` service (NOT inline `AuditLog(...)`): auth (login success/failed, logout, password change), users (register/update/deactivate/delete/profile picture/face enroll), attendance (session create/update/end, face scan time-in/out, FR consecutive-fail alert), inventory (equipment create/update, borrowing ID issued, request create/approve/release/reject/cancel/delete, borrow/return/staff-borrow/release-complete), announcements (create/update/delete/image), facilities (create/update/schedule), eligibility (create/update), incidents (create/update), sanctions (create/update/acknowledge), reports (generate/export), system (FR config update), audit (log viewed).
- Audit Logs page (`dashboard/audit-logs/page.tsx`): Admin-only guard, search, module/action/status dropdowns (from `/audit-logs/filters/*`), date range inputs (`date_from`/`date_to`), newest-first sort toggle, pagination, detail modal with JSON diff (previous/new values), CSV/XLSX/PDF export. Nav link "Audit Logs" (`ScrollText` icon) added to `dashboard/layout.tsx` with `roles: ["admin"]`.

## Work State
 ### Completed (current session)
- **FRONTEND PRODUCTION "hindi ma-click submit face enrollment" — HARDENED to max level (2026-08-31, commit `032e161`)**: User reported STILL broken even after the previous deploy fixed the stale bundle. Root-cause analysis of the deployed minified `register/page.tsx` found the actual stuck-disable bug: in old `submitFaceEnrollment`, `setFaceEnrollLoading(true)` was set OUTSIDE the `try` block, and the only reset was the `if(!valid) setFaceEnrollLoading(false); return;` path inside. If react-hook-form's `trigger()` THREW/REJECTED (resolver error on an unexpected field), we never hit the `if(!valid)` return NOR the `finally` → **`faceEnrollLoading` stuck `true` → the green "Submit Face Enrollment" button renders `disabled` forever = "hindi ma-click"**. FIXES deployed (live chunk `dca8657101a09357`): (1) rewrote `submitFaceEnrollment` so the ENTIRE body (including `trigger()`) is wrapped in `try/finally` → `setFaceEnrollLoading(false)` ALWAYS runs, button can never stay disabled; split actual API call into `performEnrollment()`; (2) added 25s `Promise.race` timeout so a hanging register/enroll request auto-fails with "The request timed out. Please check your connection and try again." instead of leaving the button frozen; (3) better errors — surfaces actual backend `detail` / `err.message` / timeout; (4) INCLUSIVE face-capture heuristic: removed the skin-tone pixel-counting gate (was `skinRatio<0.08` from a light-skin-biased `r>95&&g>40&&b>20&&r-g>15&&r-b>15` filter, silently rejecting darker skin / varied lighting so images never reached 5 → Submit button never rendered); now only sanity checks remain with lowered thresholds — brightness `<25` (was 30), contrast `<12` (was 20), skin-ratio `<0.04` (was 0.08) and skin counting removed entirely (`x=0/u`) — capture now reliably registers 5 faces for any skin tone/lighting. Verified: `tsc --noEmit` clean (ran background ~5 min, empty output); committed + pushed to `main` (2 ahead, 0 behind — no fork this time); Vercel auto-deployed; live chunk `1eaae4b1→dca8657101a09357` contains "The request timed out"(1)/"TimeoutError"(2)/"complete all missing required"(1)/"Submit Face Enrollment"(2)/"Face enrollment successful"(1) + `finally{em(!1)}` structure (loading always resets) + inclusive thresholds `p<25`/`h<12`/`x<.04`. NOTE for user: old stale-production issue means a browser **hard-refresh (Cmd+Shift+R) or incognito** is required to escape the cached PRE-fix bundle — Next.js emits new content-hashed chunk names on every deploy, so a fresh reload/incognito loads the new code; no service worker/PWA caching present (verified `grep` for serviceWorker/next-pwa/manifest = none).
- **FRONTEND PRODUCTION DEPLOYED — "hindi ma-click submit face enrollment" FIXED live (2026-08-30, commit `32eb1e9`)**: After the registration OOM/502 fix (backend deployed, `create_user` returns 201 → async Celery `enroll_face_background`, `is_face_enrolled` flips True ~30s later), the PRODUCTION frontend bundle was STALE — it still had the old `"Facial recognition enrollment failed"` error branch that sealed the flow (createdUserId set → Submit button hides; faceEnrolled never true → Continue never rendered → user stuck on step 3 with an unclickable submit). Fix was local-only; Vercel production is GIT-INTEGRATED (repo `carlkelvin8/osca-system-portal`, org, productionBranch `main`, deploy on push to `main`). **Vercel CLI deploy is NOT viable** — project framework persists as "services" mode (from a CLI scaffold incident) and Edge Runtime is unsupported in services; git push is the only reliable path. **DEPLOYMENT BLOCKED initially by a fork-of-main**: another developer (`trishvq`) had pushed 2 commits to `origin/main` — `7ecd0cf` "Fix mobile login layout" (innocuous) and **`d8be752` "Add OTP and fix registration flow" (Aug 29) which RE-INTRODUCED the entire email-OTP registration feature (WEB: backend/app/services/{email_service,otp_service}.py, models/otp.py, schemas/otp.py, migration `5f6a7b8c9d0e_add_email_otp_verification.py`, `frontend/src/app/(auth)/forgot-password/page.tsx`, `frontend/src/components/auth/OtpInput.tsx`, register/login/api/middleware changes) that we ROLLED BACK on 2026-08-11** (deployed backend has NO `/auth/registration/*` endpoints → OTP would 404 and break registration again). Merge conflict resolution (user chose "Rebase, keep no-OTP"): `git reset --hard 7ecd0cf` + `git cherry-pick f976338` (my fix) → this kept the innocuous `7ecd0cf` mobile-login fix and **DROPPED `d8be752` entirely** (deterministic — every OTP file was ADDED in `d8be752` so they deleted cleanly in the drop with zero conflicts; topped by commit `32eb1e9`, new identity of my registration fix with same 8 files + `7ecd0cf`'s login fix). Verified clean: register/page.tsx OTP markers 0, `setFaceEnrolled(true)/stopCamera/step(4)` present, auth.py `/auth/registration/*` 0, OTP files 0, `change_password` retains `request: Request` (L212). Pushed with `git push --force-with-lease origin main` (rewrote history — `d8be752` GONE from main). Vercel auto-deployed (`dpl_8ddeTmFRXM`); live chunk hash changed `b03fc82e→1eaae4b1`; live `/register` chunk now contains "Face enrollment successful — click Continue below" (1) and "Facial recognition enrollment failed" (0) — **FIX IS LIVE in production**. NOTE: prod Dockerfile on Railway for `osca-system-portal` uses the API (gunicorn) mode; the celery mode lives in the separate `osca_celery` service. NOTE 2: production backend (Railway) is NOT git-linked to this repo's backend in the same auto-deploy way as Vercel — the backend fixes were deployed via `railway up -s osca-system-portal -e production` from repo ROOT (see prior entries). Team coordination needed: trishvq re-added OTP on a separate working copy; if they force-push again it will overwrite main — communicate the no-OTP decision. Untracked `frontend/.gitignore` (contains `.vercel`, `.env*`) left untracked.
- **Registration "Registration failed" 502/OOM FIXED — face enrollment moved to background via a NEW dedicated `osca_celery` Railway worker service (2026-08-30)**: User reported registrations failing. Root cause: `POST /users/register` with 5 base64 face images (~320KB) inlined insightface buffalo_l model loading + enrollment → Railway container (1024 MB limit) OOM'd → HTTP 502 "Application failed to respond"; logs literal `Worker (pid:2) was sent SIGKILL! Perhaps out of memory?`. Diagnosis + fix per user's approved choice ("ilipat ang face enrollment sa background") → **registration now returns 201 in ~0.7 s** and enrollment happens async. Implementation: (1) new Celery task `app.workers.tasks.enroll_face_background` (bind, max_retries=2, delay 60) — decodes `face_images_base64`, `FacialRecognitionService().enroll_face`, creates `FaceEmbedding`, sets `user.is_face_enrolled=True`, commits (logs perf on success); (2) `create_user` in `users.py` replaced inline enrollment with `enroll_face_background.delay(user_id, images)` (removed unused base64/FaceEmbedding/FacialRecognitionService imports); (3) frontend `register/page.tsx` `submitFaceEnrollment` now ALWAYS proceeds to success (`setFaceEnrolled(true); stopCamera; setStep(4)`) — removed the "enrollment failed" error branch + unused `enrolled` var (`tsc --noEmit` EXIT 0); (4) `det_size` lowered 640→512 in `facial_recognition.py` L72 to trim model memory/time. **DEPLOYMENT ARCHITECTURE (prod)**: repository has only 4 Railway services (osca-system-portal, osca_minio, Redis, Postgres) and NO celery worker existed. Attempt A ran celery INSIDE the API container (`backend/start.sh`: celery `--concurrency=1 --max-memory-per-child=600000` + gunicorn; railway.toml startCommand `sh start.sh`; Dockerfile CMD `sh start.sh`) — worker came up, registration 201, but enrollment failed `WorkerLostError: Worker exited prematurely: signal 9 (SIGKILL)` (container 948→100% memory; shared gunicorn+celery+buffalo_l >1 GB) → **ABANDONED and reverted** (start.sh deleted; railway.toml startCommand back to gunicorn-only; Dockerfile git-checkout'd). FINAL setup: NEW Railway service **`osca_celery`** (production) with 55 env vars copied from the API service (ALL except auto `RAILWAY_*` and `SERVICE_MODE`) + `SERVICE_MODE=celery`; TWO needed file changes: `backend/Dockerfile` CMD is now MODE-AWARE `sh -c`: `if [ "$SERVICE_MODE" = "celery" ]; then exec celery -A app.workers.celery_app worker --loglevel=info --concurrency=1; else exec gunicorn app.main:app --worker-class uvicorn.workers.UvicornWorker --workers 1 --threads 4 --bind 0.0.0.0:8000 --timeout 180 --keep-alive 5 --access-logfile - --error-logfile -; fi` — and API `backend/railway.toml` startCommand stays plain gunicorn (unchanged). **KEY OBSERVATION**: a FRESH (empty) Railway service deployed via `railway up -s <new> -e production` from `backend/` used the Dockerfile CMD — NOT backend/railway.toml's startCommand — so SERVICE_MODE=celery was honored; the toml's startCommand only drives services that already have it stored. `railway add -s osca_celery` (interactive, Empty Service), `railway variable set -s osca_celery ...` copied vars (verified 55 set + SERVICE_MODE), deployed build `1f95cc8b` → worker `celery@10eaf4dde994` connected to `redis://default:**@redis.railway.internal:6379/1`, all tasks registered incl. `app.workers.tasks.enroll_face_background`. VERIFIED END-TO-END: earlier-queued real-face registration task succeeded in 30.2 s (`Task ... succeeded in 30.239037262275815s`); then a FRESH registration with 5 real face images (Lena) → `register: 201 in 0.7s` (user `5340ed62...`), polled `GET /users/{id}` → `is_face_enrolled=False` @15 s, **`True` @30 s (ENROLLED OK)**. Metrics after fix: API container 271 MB (26%) — back to normal; celery worker peak 1023/1024 MB (enrollment legitimately needs ~1 GB; concurrency=1 avoids parallel-load spikes; NO CLI resize exists (`railway service` has no resize cmd) → if headroom needed later, bump via dashboard slider). Railway CLI also gained piped `printf "\n" | railway add` quirk + note `railway config init` created a DESTRUCTIVE blank `.railway/railway.ts` scaffold (project "osca-management-system") — NOT applied; scaffold files deleted (`.railway/` back to empty). Test users ALL purged via `DELETE /users/{id}/permanent`: `zz_finale25af3@test.ph` (5340ed62), `zz_bgenroll9da8c3@test.ph` (a5e7a44f), `zz_bgenroll_real@test.ph` (f7e86cc0 — the Lena-enrolled one); API confirms **0 `zz` users, 13 real users** (`nur.umpa@osca.naap.edu.ph` admin = pre-existing, kept). `py_compile` clean (users/tasks/facial_recognition/storage/auth). Git now: `backend/Dockerfile` (mode-aware CMD) + `users.py` + `tasks.py` + `facial_recognition.py` + `register/page.tsx` + `AGENTS.md` modified; `frontend/.gitignore` + `frontend/tsconfig.tsbuildinfo` pre-existing; `backend/start.sh` deleted. NOTE: orphaned MinIO `enrollments/{user_id}/...` objects for the deleted test users remain (tiny; permanent-delete doesn't remove MinIO objects).
- **MinIO presigned URL scheme-duplication bug FIXED + DEPLOYED — ALL MinIO images were broken in production** (`backend/app/services/storage_service.py`): User reported "announcement creation still shows no image." Root cause: `get_presigned_url` builds the public endpoint as `f"{self._scheme}://{public_ep}"`, but production's `MINIO_PUBLIC_ENDPOINT` value **already includes the scheme** (`https://oscaminio-production.up.railway.app`) → every presigned URL came out as the malformed `http://https://oscaminio-production...` (double scheme, invalid host, `nodename nor servname provided` DNS error on fetch). This broke ALL MinIO-served images in production: announcement images, venue/facility images, profile pictures, face images (only the internal-endpoint fast path `scheme != https and public_ep == MINIO_ENDPOINT` was correct). FIX: if `"://" in public_ep` use it verbatim as the boto3 endpoint_url, else join with `scheme://`. Only line 100 changed (+7/-1). `py_compile` clean; deployed to Railway from repo ROOT via `railway up -d -y -s osca-system-portal -e production`. VERIFIED live post-deploy (multi-part upload to `POST /announcements/{id}/image` with proper `multipart/form-data; boundary=` header — failed 422 earlier due to test's wrong Content-Type): created announcement → 201, uploaded 1px PNG → 200 (returns raw key `announcements/{ann_id}/{hash}.png`), `GET /announcements` list now returns a CLEAN presigned URL `https://oscaminio-production.up.railway.app/osca-reports/announcements/{ann_id}/{hash}.png?X-Amz-...` (was `http://https://...`), and fetching that URL returns **200 image/png** (was DNS error). 3 temp `zz imagetest` announcements deleted (0 remain; prod back to the 1 pre-existing mojibake public_website row). No frontend change needed — the fix is purely backend presigning. Git: `backend/app/services/storage_service.py` + `AGENTS.md` modified (plus prior `auth.py`); untracked `frontend/.gitignore` pre-existing.
- **Production end-to-end test sweep + 2 bug fixes (Railway deploy, 2026-08-30)**: Comprehensive testing of ALL backend endpoints against production (`osca-system-portal-production.up.railway.app`). Found and fixed **2 real production bugs**: (1) `request_status_enum` in the production Postgres had lowercase members `pending`/`approved`/`rejected` (only `CANCELLED` was uppercase) while SQLAlchemy writes uppercase member names → **every `POST /inventory/requests` (coach equipment request) returned 500** `InvalidTextRepresentationError: invalid input value for enum request_status_enum`. FIXED directly in the production DB: `ALTER TYPE request_status_enum ADD VALUE 'PENDING'`/`'APPROVED'`/`'REJECTED'`. Verify: coach creates request → 201, admin approve → 200, complete transaction → 200, reject → 200, cancel → 200, delete → 200. (2) `PUT /auth/me/password` returned **500 even with correct current password** — root cause: `change_password` in `backend/app/api/v1/auth.py` calls `audit_log(..., request=request)` but lacked the `request: Request` param → `NameError`. FIXED with a one-line signature change (`request: Request,` added at L212) and DEPLOYED to Railway via `railway up -d -y -s osca-system-portal -e production` from repo ROOT (deploy must be from root since `backend/railway.toml` points at `backend/Dockerfile`). Verified post-deploy: change-password → 200 (note: first attempt after deploy hit the still-running PRE-deploy container → 500; re-run after rollover → 200). Admin password restored to `OSCAAdmin2026!` + login verified. **Full write-endpoint sweep (all 201/200/204 after fixes)**: equipment create, session create/update(PATCH)/end/open/delete, announcement create/PATCH/delete + acknowledge + comment, eligibility create/PATCH, incident create/PATCH/DELETE, sanction create/PATCH/DELETE, facility create/PATCH/DELETE, equipment request lifecycle, reservations submit(POST, coach)/approve/reject, notifications read-all, manual attendance add/edit/delete, report exports (csv/xlsx/pdf all 200), audit exports (`/audit-logs/export/{csv,xlsx,pdf}` all 200). By-design behaviors confirmed (NOT bugs): 405 on update endpoints when using PUT (they're PATCH), 400 "Session is already open" (fresh sessions are active; `open` only re-opens closed sessions), 403 reservation-submit by admin (coach/PE only), 405 `DELETE /eligibility/{id}` (eligibility has NO delete endpoint — feature gap, frontend never calls it), 409 admin release-after-approve (approve and release are alternative paths), `/users/me` → 422 (path is `/users/{user_id}`, no `me` shortcut). ALL temp `zz_*` test data purged (users, facilities, equipment, sessions, announcements+acks+comments, eligibility, incidents, reservations, audit rows) — verified 0 zz across all tables + API confirms 12 production users, 0 zz. Git: only `backend/app/api/v1/auth.py` modified (one-line fix).
- **Public website News & Announcements carousel REDESIGNED to a STRICTLY HORIZONTAL center-focused cover-flow** (`frontend/src/app/welcome/WelcomeClient.tsx`, UI only): the `#news` card grid was replaced with a new `NewsCarousel` component + `CarouselCard`. 3 cards sit in ONE single horizontal row (all `top: 0`/`offsetTop: 0`, distinct X centers) — center is focused (opacity 1, `blur(0)`, scale 1.0, z-index 3, pointer-events auto) and sides are smaller (scale 0.9), blurred (`blur(3.5px)`), faded (opacity 0.55). All items stay in the DOM; each card is absolutely positioned and `translateX(calc(-50% + ${delta * offset}px)) scale(...)` (NO translateY) with wrap-normalized `delta` (circular distance) so loop transitions animate smoothly; CSS transition `transform 0.7s cubic-bezier(0.4,0,0.2,1), opacity 0.6s ease, filter 0.6s ease`. **Sides stay RECOGNIZABLE (mostly fully visible, NOT clipped behind center)**: **center card is ~28% WIDER than the base/side cards** — `centerW = round(cardW * 1.28)` (mobile factor 1) applied as `width: focused ? centerW : cardW`; side offset = `round(centerW/2 + cardW*0.45 - 22)` so the center overlaps each side's inner ~22px via z-index 3; container is WIDE (`min(2*(offset + sideHalf) + 12, max(600, winW-40))` where `sideHalf = cardW*0.9/2` — 954px desktop) so both side cards fit fully inside with zero clipping (verified 288/288 visible; center 410px, sides 320px base/288px scaled). **NO arrow buttons** (chevron buttons removed entirely; `prev`/`arrowTop` deleted). **Side cards are CLICKABLE**: adjacent cards get `onClick={() => setActive(i)}` + `cursor: pointer` + `pointerEvents: auto`, and their inner `CarouselCard` is wrapped in a `pointerEvents: none` div so clicking anywhere on a side card smoothly moves it to center (no lightbox/anchor hijack from sides); clicking LEFT → becomes center, clicking RIGHT → becomes center (verified with real CDP mouse clicks). Center card stays fully interactive (image → lightbox, View Link anchor). Auto-advances every 6000ms (`setInterval`, `n<=1` or hover-paused skips), loops (modular wrap), initial center = index 0 of the public feed (newest per feed order — NO randomization). Pagination dots underneath (gold pill `#C9A84C` 24px active / `#d9dce2` 8px, clickable). Responsive via `winW` state (resize listener): desktop cardW 320 / tablet 280 (`640<=w<1024`) / mobile = `min(360, max(280, winW*0.86))`; mobile (`<640`) hides side cards (opacity 0) — single sharp center card only. Content preserved exactly: image (click → `PublicImageViewer` lightbox via `onOpenLightbox`, `cursor: zoom-in`), `+N more` badge when `images.length>1`, gold date span, navy title + excerpt (`WebkitLineClamp` 2/3 to keep card heights fixed), `View Link` + whole body `<a target="_blank">` when `href` set. `newsItems` still sourced from `GET /announcements/public` (Publish To filter already only `public_website`/`both` — NO backend/DB change). Empty-state check changed from `NEWS_ITEMS.length` → `newsItems.length` (dead branch, `newsItems` always falls back to `NEWS_ITEMS`). VERIFIED live in headless Edge (CDP): **v3 desktop/horizontal test** (`horiz_test.mjs` + `horiz_click_test.mjs`, width-emulated 1440 + temp 3rd public announcement `zz-horiz-mid`): 3 cards in one row (offsetTops [0,0,0], distinct X centers [431,713,995]), sides smaller/blurred/faded, center sharp, ARROWS=0, dots=3 match + clickable, click LEFT card → becomes center (real mouse), click RIGHT card → becomes center (real mouse), center image click → lightbox opens, auto-slide advances (dotW [8,8,24] proves active moves); **v4 wider-center test** (`wider_test.mjs`): center offsetWidth 410 vs base 320 (ratio 1.281 ✓), side offsetWidth stays 320 (rect 288 ✓), container 954px fits window, all cards inside container, same top row, center sharp + dominant z, sides blurred/faded; mobile 390 (`horiz_mobile_test.mjs`) → single sharp center (335px). Screenshots `carousel_desktop.png`/`carousel_mobile.png` captured in earlier iteration (model can't view images — DOM checks are ground truth). NOTE: CDP `Input.dispatchMouseEvent` needs the target scrolled into view first (viewport 900px tall vs cards at y≈1500 → out-of-viewport clicks are unreliable); synthetic `dispatchEvent('click')` on side cards did NOT register consistently — use real mouse input. Test data (`zz-horiz-mid`, psql-inserted) purged (0 `zz-*` remain); public feed back to original 2 items. `tsc --noEmit` EXIT 0; `.next` wiped + `osca_frontend` restarted.
- **Announcement Management page (complete, backend + frontend, E2E pending)** (`backend/app/api/v1/announcements.py` + `frontend/src/app/dashboard/announcements/page.tsx` + shared form): NEW `GET /announcements/manage` (auth + `_EDITOR_ROLES` admin/director/staff) with params `search` (title/content ILIKE), `publish_to` (all_dashboards/public_website/both), `status` (active/inactive), `sort` (newest/oldest), `page`/`page_size`; reuses `_build_announcement_page`. Relaxed `is_active` guards so INACTIVE announcements can be updated/deleted (update/delete/upload-image/remove-image: `if not ann` instead of `if not ann or not ann.is_active`); ack/comment routes keep the active guard. Error messages standardized to "Only admin, director, and staff may {create/update/delete/manage} announcements." Added `or_` to the sqlalchemy import. Frontend: `announcementsApi.manageList` in `lib/api.ts`; shared `AnnouncementFormModal` extracted from dashboard page into `frontend/src/components/announcements/AnnouncementFormModal.tsx` (exports `AnnouncementFormModal`, `tagConfig`, `VISIBILITY_OPTIONS`, `announcementImages`; internal `MAX_IMAGE_SIZE`/`ALLOWED_IMAGE_TYPES`), dashboard page imports it (local form/tagConfig/VISIBILITY_OPTIONS removed); new `/dashboard/announcements` page (editor-role guard, header + Create button, search input + Publish To/Status/Sort dropdowns, table [Title+preview | Publish To badge | Date Posted | Status | Created By | Actions], pagination, Edit via shared modal, Delete confirm dialog "This announcement will be removed from all locations where it is currently published."). Nav: `layout.tsx` adds "Announcements" (`Megaphone` icon) for admin/director/staff after "Users"; middleware `access_token` cookie already covers it. VERIFIED: `py_compile` + `tsc --noEmit` clean; live API — manage list total 10 (4 active: 3 all_dashboards + 1 public_website mojibake; 6 inactive), filters (status inactive→6/active→4, publish_to public_website→1, search HELLO→1, sort orders correct), coach → 403, inactive edit/delete OK. E2E script `ann_manage_test.mjs` (CDP, 15 checks) TIMED OUT on the shell at 300s after `SETUP` (login+cookie OK; `/dashboard/announcements` first Turbopack compile slow) — rerun with longer timeout / pre-compile route to finish. Temp coach + `zz_managetest` announcement + audit rows purged (0 remain). NOTE: DB still holds pre-existing leftover dev rows: active "wq"/"t"/"HELLO" (all_dashboards) + active **mojibake (corrupted UTF-8) public_website** announcement (appears on the public site!), inactive "lih"/"Test"/"MULTIIMAGE TEST"×2/"LOGO"/"NAP" — flagged to user, not cleaned.

### Completed (current session)
- **Announcements "Publish To" visibility + optional URL + multi-image public feed (complete, live-verified)**: ONE shared announcement system — a record is no longer per-dashboard, `visibility` decides where it shows. Backend (`announcements.py` + model + schema): `announcements.visibility` String(20) default `all_dashboards` + `announcements.link_url` String(500) (model `default`/`server_default`, `AnnouncementVisibility = Literal["all_dashboards","public_website","both"]`, create/update/read fields; all 9 existing rows defaulted to `all_dashboards` via migration). `list_announcements` filters `visibility IN (all_dashboards,both)` + optional `publish_to` query param; NEW unauthenticated `GET /announcements/public` filters active + `visibility IN (public_website,both)`, `page_size` max 100 (default 50); shared helper `_build_announcement_page(db, query, page, page_size, viewer)`; create sets visibility/link_url + audit `new_values`. Migration `3e4f5a6b7c8d` (down `2a3b4c5d6e7f`) applied — NOTE: DB was actually at `2a3b4c5d6e7f` (old `1f2e3d4c5b6a` head note was stale; first attempt failed with "Multiple head revisions" until down_revision fixed). Frontend: `types/index.ts` adds optional `visibility?`/`link_url?`; `api.ts` `announcementsApi.publicList` uses BARE axios (avoids 401 redirect for anonymous welcome visitors); dashboard form gains **Publish To** radios (All Dashboards / Public Website / Both + descs) + Link/URL input, card becomes clickable → opens link in new tab when `link_url` set (header actions/footer/PhotoGrid `stopPropagation` so controls don't navigate), visibility badge (Globe) + "Open Link" chip, viewer adds Open Link anchor; welcome page (`WelcomeClient.tsx`) fetches `/announcements/public` via React Query, renders DB news cards (fallback `NEWS_ITEMS` when empty), `+N more` badge for multi-image, plain `<img>` for MinIO presigned URLs (next/image can't), click image → new portal lightbox `PublicImageViewer` (prev/next + counter "i / n", Esc/arrows, click-outside), carousel now fed from public data (fallback `ANNOUNCEMENTS`). VERIFIED end-to-end: `py_compile` + `tsc --noEmit` clean; live API create both→public+dashboard visible, patch→public_website drops from dashboard list, delete removes; multi-image upload → 2 presigned `image_urls` (200); headless Edge (CDP) on `/welcome`: card renders title/image/`+1 more`/`View Link`, anchor `target=_blank href=link_url`, carousel shows title, image click → lightbox `1 / 2` → next → `2 / 2` → Esc closes. All zz_* test rows/audit/MinIO objects purged (0 remaining). NOTE: `celerybeat-schedule` + `tsconfig.tsbuildinfo` show as modified build artifacts — not staged. One pre-existing stray audit row `USER_LOGIN_FAILED zz_idtest@osca.naap.edu.ph` (from Digital ID session) remains — harmless.
- **Download Digital ID FIXED (root cause: dependency never installed)** (`frontend/src/app/dashboard/profile/page.tsx`): `dom-to-image-more@^3.10.2` was declared in `package.json` but NOT present in the `node_modules` NAMED VOLUME → Turbopack compiled `await import("dom-to-image-more")` in `handleDownload` into a `MODULE_NOT_FOUND` stub (`const { toPng } = await (()=>{ throw new Error("Cannot find module 'dom-to-image-more'"); })()`) that always threw, and the app's `catch {}` swallowed it → "Download Digital ID" silently did nothing (no console error, no download). FIX: `docker exec osca_frontend sh -c "cd /app && npm install dom-to-image-more@3.10.2 --no-audit --no-fund"` (added 1 package; `node_modules` volume persists across restarts; `package.json`/`package-lock.json` UNCHANGED since the range already matched — `git status` clean for those). Then `.next` wiped + `osca_frontend` restarted; compiled chunks now contain the real `node_modules_dom-to-image-more_dist_*.js` modules (stub count 0). VERIFIED end-to-end in headless Edge (CDP + cookie injection; temp coach `zz_idtest@osca.naap.edu.ph` created via `POST /users/register` with `role=coach` — BorrowingID `BID-{uuid}` auto-issued — then permanently deleted; 0 zz data remains): card renders, click produces a valid PNG (493×253, 32,501 bytes, signature OK) data URL via `toPng`, `<a download="digital-id-*.png">` click fires + `Page.downloadWillBegin` (headless `data:` downloads end `canceled` — headless artifact, works in real browsers). MinIO presigned profile-pic URLs verified to return `Access-Control-Allow-Origin: http://localhost` (200, `image/png`) so CORS is NOT a blocker for the cross-origin avatar. Test infra (temp dir): `dlid_test*.mjs`/`dlid_diag.mjs`/`dlid_capture.mjs` CDP scripts + `zz_downloads`; note `Runtime.evaluate` `import('dom-to-image-more')` fails with bare-specifier resolve error (Turbopack rewrites only app-side imports — use anchor-click instrumentation to observe).
- **PDF reports now LEGAL size + LANDSCAPE** (all PDF exports): changed the 4 `@page` CSS blocks from `size: A4 landscape` → `size: legal landscape` (margins/styling/data/columns untouched): `backend/app/api/v1/reports.py` (×2 — `_build_attendance_html` L344 + `_rows_to_pdf` L606), `backend/app/api/v1/audit_logs.py` L389, `backend/app/services/report_service.py` `_html_to_pdf` L375 (covers `/reports/attendance/pdf`, `/reports/inventory/pdf`, `/reports/inventory/monthly`). `legal` is a valid xhtml2pdf page size (reportlab `LEGAL`). VERIFIED: hit all 23 PDF endpoints (daily/weekly/monthly attendance, inventory equipment/borrowing-history/returned/lost-damaged/monthly, facilities venue-reservations/venue-usage/status, eligibility ×3, incidents ×3, sanctions ×3, attendance/pdf, inventory/pdf, audit-logs/export/pdf) → every `MediaBox` = `1008 × 612 pt` (Legal 8.5"×14" landscape). Zero `A4`/`Letter` left in report code. CSV/XLSX endpoints unchanged and verified 200. No comments added; the 3 modified files contain zero comments (all `#` hits are CSS hex colors). `py_compile` + `tsc --noEmit` clean; `osca_api`/`osca_celery`/`osca_celery_beat` restarted.
- **Dashboard: Equipment Status REMOVED for coach AND PE Instructor** (`frontend/src/app/dashboard/page.tsx` `RoleView`): the RoleView Equipment Status card is gone entirely; Attendance This Week now spans full width (`className="lg:col-span-12"`) for coach/pe_instructor/student (no empty gap). ManagerView (admin/director/staff) still shows its Equipment Status card (L1387). Only `page.tsx` changed. Verified: `tsc --noEmit` clean; `.next` wiped + `osca_frontend` restart; compiled chunk `src_app_dashboard_page_tsx_5b2001._.js` has exactly ONE "Equipment Status" (ManagerView), no `pe_instructor` gate, RoleView attendance card `lg:col-span-12`.
- **Email OTP feature REMOVED — registration flow ROLLED BACK to pre-OTP behavior (2026-08-11)**: All OTP code deleted and the DB restored. Backend: removed `EmailOtp` model (`models/otp.py`), `email_service.py`, `scripts/diag_smtp.py`, OTP schemas (`OtpSendRequest`/`OtpVerifyRequest`/`OtpSendResponse` in `schemas/auth.py`), `/auth/registration/send-otp` + `/verify-otp` endpoints in `auth.py` (now 404), `users.email_verified` column (model + `schemas/user.py` `UserRead`/`UserSummary`), OTP gate + `email_verified=True` + OTP-row cleanup in `users.py` `create_user`, SMTP alias settings (`SMTP_USERNAME`/`SMTP_FROM`) + all `OTP_*` settings in `config.py`. Frontend: deleted `OtpVerificationModal.tsx`, removed OTP modal/state/handlers/countdown + `verifiedEmailRef` gate from `register/page.tsx`, removed `authApi.sendRegistrationOtp`/`verifyRegistrationOtp` from `lib/api.ts`, restored `register/success/page.tsx` message to "Your account has been created and is now pending approval." + "You will be able to sign in once an administrator activates your account." DB: dropped `email_otps` table + `users.email_verified` column, deleted migrations `a2b3c4d5e6f7`/`b2c3d4e5f6a7`, re-stamped `alembic_version` to `1f2e3d4c5b6a` (`alembic current` = `1f2e3d4c5b6a` = head), purged `otp_*` Redis keys + `REGISTRATION_OTP_*` audit rows. KEPT (non-OTP): Student ID strict format `^\d{5}[A-Z]{2}-\d{6}$` + auto-uppercase in `register/page.tsx`, all kiosk latest-attendance work (`ScanAttempt.session_id` migration `1f2e3d4c5b6a`, `getSessionLatest`, etc.), `.env` untouched (SMTP_* vars pre-existed, unused). Verified live: `/auth/registration/*` → 404; `POST /users/register` (no OTP) → 201 with `is_active:false` (PENDING); login → 403 pending approval; temp test user + audit rows cleaned (0 remaining); `py_compile` EXIT 0; `tsc --noEmit` EXIT 0; `.next` wiped + `osca_frontend` restart; `/register` → 200 with fresh chunks containing ZERO OTP markers. `git status` shows OTP files fully gone (auth.py/config.py/models/__init__.py/models/user.py/schemas/auth.py/schemas/user.py/success/page.tsx back to HEAD; users.py identical to HEAD; register/page.tsx retains only the Student ID change).
- **Student ID field format** (`frontend/src/app/(auth)/register/page.tsx`): registration Student ID now strict `^\d{5}[A-Z]{2}-\d{6}$` (e.g. `12345MN-000000`); placeholder updated to `12345MN-000000`; `maxLength={14}`; letters auto-uppercased via `register("student_id", { onChange })`; error "Student ID must follow the format 12345MN-000000 (5 digits, 2 uppercase letters, hyphen, 6 digits)". Form otherwise untouched. `tsc --noEmit` EXIT 0; `/register` â†’ 200; compiled chunk `src_d41f16._.js` has new placeholder/validation, old `e.g. 2024-0001` count 0.
- **Kiosk "Last successful attendance" now persisted (DB-backed, not local state)** (`frontend/src/app/kiosk/page.tsx` + `backend/app/api/v1/attendance.py`): The kiosk card previously showed only the last scan from the current React session (lost on refresh). Now it fetches from a new endpoint `GET /attendance/sessions/{id}/latest-attendance` (`LatestAttendanceRead` schema) so it survives page refresh and reflects everyone who scanned. Backend: `ScanAttempt` gained nullable `session_id` FK â†’ `sessions.id` `ON DELETE CASCADE` (migration `1f2e3d4c5b6a` â€” new head, down_revision `d7e8f9a0b1c2`; applied); `face_scan` now stores `session_id` on every attempt. Endpoint logic (PE instructor 403, coach-scope enforced via `_enforce_coach_session_scope`): (1) prefers latest successful `ScanAttempt` for the session (join `users` for name/role) â€” if the matched user is a STUDENT it enriches with their `AttendanceRecord` (status present/late, time_out, duration); non-students return identity-only (role, scan time, no status/record, matching the staff-recognition feature); (2) fallback to latest `AttendanceRecord` (covers pre-session_id scan history); (3) else `has_record: false`. Frontend: `LatestAttendance` type + `attendanceApi.getSessionLatest`; kiosk card uses `useQuery(["kiosk-latest-attendance", selectedSessionId])` (`placeholderData` prev to avoid flicker) instead of the removed `lastSuccess` state; on scan success it invalidates both `kiosk-latest-attendance` and `kiosk-session-stats` so the card + Today's overview update immediately; card shows name, scan time, session/sport line, and badge â€” "Present"/"Late" for students, role uppercase (blue) for staff, empty state when `has_record` false. Verified: `py_compile` + `tsc --noEmit` EXIT 0; migration applied (`alembic current` = `1f2e3d4c5b6a`); live-tested via temp accounts/session (created in DB + deleted after): student scan â†’ `person_role: student` + `status: present` + time/session/sport/confidence; later staff scan â†’ `person_role: admin`, `status: null`; empty session â†’ `has_record: false`; all zz_* temp data cleaned (0 users/scans/sessions remaining); `osca_frontend` restarted; `/kiosk` â†’ 200; compiled chunk `src_9e24f9._.js` (kiosk page inlined, verified on local `docker cp` copy) contains `kiosk-latest-attendance`(2)/`getSessionLatest`(2)/`has_record`(1)/`latestSessionLine`(3)/`No attendance recorded yet`(1)/`Last successful attendance`(3). NOTE: container-internal `grep` on minified chunks is unreliable (false negatives on plain-text substrings) â€” verify via `docker cp` + local PowerShell `[regex]::Matches`.
- **Registered face recognition for Admin/Director/Staff/Coach** (complete, live-verified): Admin/Director/Staff/Coach/PE Instructor can now register their face and be recognized at the kiosk for identity verification only â€” NO student attendance record is ever created for them. `enroll_face` (`attendance.py`): any user may now enroll their OWN face (new `is_self` early-allow, so coaches/staff can self-enroll); students still only their own; coaches still only their assigned-sport students; admin enrolls anyone; consent error message made role-agnostic ("User has not provided biometric consent (R.A. 10173)"). `face_scan`: after match + inactive-account check, new branch for `matched_user.role != STUDENT` â†’ audit `FACE_RECOGNIZED_STAFF` (details: role, confidence, `attendance_recorded: false`) + returns `SUCCESS` with name + role, no AttendanceRecord; staff skip the sanction check (students-only). Student time-in/time-out + duplicate responses now also carry `matched_user_role` (schema `FaceScanResponse` + frontend `FaceScanResponse` type gained `matched_user_role: str | None`). **Ambiguity-safe matching** (`services/facial_recognition.py` `identify_face`): tracks top-2 scores; if best â‰¥ threshold but `best - second < AMBIGUITY_MARGIN` (0.02) â†’ returns `failed_recognition` "Multiple possible matches foundâ€¦" instead of guessing (applies to students too). **Robustness fix (pre-existing 500)**: `_decode_image` `ValueError` on corrupt/garbage frames now caught in `_run` â†’ `no_face_detected` "Invalid image frameâ€¦" (was an unhandled 500). Frontend (`kiosk/page.tsx`): `isStaff = matched_user_role && !== "student"` â†’ overlay "Face Recognized!" (vs "Time-In/Time-Out Recorded!"), subtitle mentions personnel identity verification, "Last successful attendance" badge shows role (blue `bg-blue-100 text-blue-700`, `role.replace("_"," ").toUpperCase()`) instead of "Present" for staff. Verified live with temp accounts (created directly in DB + deleted after): coach SELF-enroll â†’ 422 "Failed to decode image" (permission passes, was 403), coach enroll out-of-sport â†’ 403, admin enroll coach â†’ 422, garbage scan â†’ 200 `no_face_detected`; `py_compile` + `tsc --noEmit` pass; OpenAPI `FaceScanResponse` has `matched_user_role`; `.next` wiped + recompiled chunk `src_9e24f9._.js` contains `Face Recognized`/`matched_user_role`/`identity verification only`/role badge; all 8 containers healthy after a `docker compose up -d` (Docker Desktop had dropped api/postgres/redis/minio/celery/celery_beat/nginx). Temp test data cleaned up (zz_* users, ZZ Temp Session + its attendance record for trisha@gmail.com that was scanned in during the active test window).
- **Dashboard LAYOUT standardization â€” Student is the visual reference** (`frontend/src/app/dashboard/page.tsx`, UI only): All dashboards now share the Student dashboard's layout system (full-width, no `mx-auto max-w-7xl`), while keeping 100% role-specific content. Changes made (Student itself UNTOUCHED): (1) removed `mx-auto max-w-7xl space-y-7` wrapper â†’ `space-y-7` so Admin/Director/Staff/Coach/PE Instructor dashboards span the same full width as Student; (2) `WelcomeSection` wrapped in a card (`${CARD} p-6`) so non-student welcome matches Student's welcome card exactly (same radius/border/padding/shadow); (3) `ManagerView` Attendance chart + Quick Actions row changed from `lg:grid-cols-3` + `lg:col-span-2` â†’ `lg:grid-cols-12` with chart `lg:col-span-7 xl:col-span-8` and Quick Actions `lg:col-span-5 xl:col-span-4 h-full` (matches Student's `lg:grid-cols-12` 8/4 split); (4) `RoleView` charts row changed from `lg:grid-cols-2` 50/50 â†’ `lg:grid-cols-12` with Attendance `lg:col-span-7 xl:col-span-8` + Equipment `lg:col-span-5 xl:col-span-4`. Already-matching tokens (unchanged): stat grids `grid grid-cols-1 gap-5 sm:grid-cols-2 xl:grid-cols-4` + `wide` StatCards, `gap-6` on 2-col rows, `h-[240px]` charts, `SectionCard`/`CARD` radius/padding/shadow. Verified: `tsc --noEmit` EXIT 0; `.next` wiped + `osca_frontend` restart; `/dashboard` â†’ 200; compiled chunk `src_app_dashboard_page_tsx_5b2001._.js` (len 324034) contains `lg:grid-cols-12`(3)/`lg:col-span-7 xl:col-span-8`(3)/`lg:col-span-5 xl:col-span-4`(3)/`${CARD} p-6` welcome/Student's inline welcome card intact (`rounded-2xl border border-gray-100 bg-white p-6 shadow-[0_1px_2px...0,0_12px_32px...]`)(1)/"My Attendance"(1)/"Quick Actions"(1) and `max-w-7xl`(0)/`lg:grid-cols-3`(0)/`lg:col-span-2`(0). No backend/API/permission/route/nav/banner/breadcrumb changes.
- **Dashboard design-system refactor (cards + charts)** (`frontend/src/app/dashboard/page.tsx`, UI only): Introduced shared primitives after `tagConfig` â€” `CARD` (soft white card class), `TONES` (chip classes per accent), `toneFromLegacy()` (maps legacy `bg-{color}-500` â†’ tone), `SectionCard` (title/subtitle/icon/action header + card), `StatCard` (tone chip + big value + label/sub), `MiniStat` (inline count pill), `StatusBadge` (present/late/absent/excused chip). Replaced the old `KpiCard` (gradient icon squares, `iconCls`) with `StatCard` + `tone` in BOTH `ManagerView` (Total Students=blue, Attendance Today=green, Equipment Available=indigo, Overdue Returns=red/gray-conditional + conditional `text-red-600`) and `RoleView` (tone derived via `toneFromLegacy(stat.color)`). Wrapped the Attendance This Week + Equipment Status charts and the Quick Actions panel in `SectionCard` (icons TrendingUp/Package/LayoutDashboard, subtitles, kept the admin-only "Manage sessions" / "Manage inventory" links as `action`). Equipment charts now render per-bar colors via recharts `Cell` using the pre-existing `fill` field on `equipmentChartData` (Available=`#22c55e`, Borrowed=`#6366f1`); prop types updated to `{ name; qty; fill }[]`. Added `MiniStat` pills (Available/Borrowed counts) above both equipment charts. `StatusBadge` kept as an (unused) primitive â€” no `noUnusedLocals`, so tsc passes. Verified: `tsc --noEmit` EXIT 0; `.next` wiped + `osca_frontend` restart (first authed `/dashboard` request 504 while Turbopack compiled ~123s, then 200); compiled chunk `src_app_dashboard_page_tsx_5b2001._.js` contains `SectionCard`(8)/`StatCard`(8)/`MiniStat`(5)/`toneFromLegacy`(2)/"Attendance This Week"/"Equipment Status"/"Quick Actions"/"Your scans from the past seven days" and `KpiCard`=0. No backend/API/permission changes.
- **Dashboard layout â€” charts full-width + Quick Actions right** (`frontend/src/app/dashboard/page.tsx` `ManagerView`): New arrangement per user request â€” KPI stat row â†’ `lg:grid-cols-3` [Attendance This Week chart (col-span-2) | Quick Actions (right column, `h-full`)] â†’ Equipment Status chart FULL-WIDTH as its own row (`h-[240px]`, MiniStat pills + per-bar `Cell` colors) â†’ Announcements. Previously the Equipment chart sat stacked under Attendance inside the 2/3 column. RoleView untouched (no Quick Actions there). Verified: `tsc --noEmit` EXIT 0; `.next` wiped + `osca_frontend` restart; `/dashboard` â†’ 200; compiled chunk `src_app_dashboard_page_tsx_5b2001._.js` (len 222067) contains `h-[240px]`(1)/`h-[220px]`(3)/`SectionCard`(8)/`Equipment Status`(2), `KpiCard`=0.
- **Dashboard header sticky** (`frontend/src/app/dashboard/page.tsx` `WelcomeHeader`): Header now sticks to the top while scrolling. Changed ONLY the wrapper class `relative` â†’ `sticky top-0 z-30` (all other classes `-mx-5 -mt-5 mb-6 overflow-hidden lg:-mx-7 lg:-mt-7 lg:mb-7` unchanged; design/NAAP/logo/wave/clock untouched). Works because `<main className="flex-1 overflow-auto">` in layout.tsx is the scroll container (topbar stays above it, content scrolls under the header). IMPORTANT: verified empirically with headless Edge (temp `sticky_test.html`) that Chrome/Edge stick the border box flush at the scrollport with `sticky; top: 0` + negative top margins â€” delta=0 at rest AND scrolled (no gap, no clip) â€” so no `top` compensation needed. z-30 sits above scrolling cards but below modals (z-50+). Verified: `tsc --noEmit` EXIT 0; `.next` wiped + restart; `/dashboard` â†’ 200; compiled chunk `src_app_dashboard_page_tsx_5b2001._.js` contains `sticky top-0 z-30 -mx-5 -mt-5 mb-6 overflow-hidden lg:-mx-7 lg:-mt-7 lg:mb-7`(1).
- **Dashboard header FLATTENED â€” no more card look** (`frontend/src/app/dashboard/page.tsx` `WelcomeHeader`): Removed `rounded-r-[18px]`, `shadow-md`, and `ring-1 ring-black/5` from the header wrapper â†’ it is now a plain/flat full-width banner with NO rounded corners, NO drop shadow, NO floating-card look. Wrapper stays `relative -mx-5 -mt-5 mb-6 overflow-hidden lg:-mx-7 lg:-mt-7 lg:mb-7` (full-bleed, flush against sidebar/topbar edges, `overflow-hidden` kept to clip the wave + photo). Everything else untouched: `url('/NAAP.png')` cover + `center`, navy overlay `from-[#061a38]/95 via-[#123b68]/75 to-[#123b68]/45`, transparent logo (`h-12 w-12 md:h-16 md:w-16 object-contain`, no white box), white branding text, glass clock panel (`rounded-2xl` â€” the only remaining shadow is the clock panel's own `shadow-lg`), wave SVG `M0,65 C240,20 480,85 720,55 C960,25 1200,80 1440,45...` fill `#f2f5f9`, `h-[150px] md:h-[165px]`. Verified: `tsc --noEmit` EXIT 0; `.next` wiped + `osca_frontend` restart; compiled chunk `src_app_dashboard_page_tsx_5b2001._.js` header wrapper = `relative -mx-5 -mt-5 mb-6 overflow-hidden lg:-mx-7 lg:-mt-7 lg:mb-7` (no rounded/shadow/ring); `System Online`=0.
- **Dashboard header full-width positioning** (`frontend/src/app/dashboard/page.tsx`): `WelcomeHeader` moved OUT of the `mx-auto max-w-7xl` container in `DashboardPage`'s return â€” it is now a direct sibling rendered BEFORE the content container, so it is no longer a centered floating card. Header wrapper uses negative margins `-mx-5 -mt-5 mb-6 lg:-mx-7 lg:-mt-7 lg:mb-7` to break out of `<main className="p-5 lg:p-7">` (layout.tsx) â€” header now spans the ENTIRE main content width and is flush directly against the left sidebar / topbar boundary (no left gap, no centering). Border radius changed from `rounded-[18px]` (all corners) to `rounded-r-[18px]` (right corners only, left edge flush with sidebar). Fixed height `h-[150px] md:h-[165px]` (was `min-h-[150px] md:min-h-[160px]`). Everything else unchanged: `url('/NAAP.png')` cover + `center`, navy overlay `from-[#061a38]/95 via-[#123b68]/75 to-[#123b68]/45`, transparent logo (`h-12 w-12 md:h-16 md:w-16 object-contain`), branding text, glass clock (`rounded-2xl`), wave SVG (`M0,65 C240,20 480,85 720,55 C960,25 1200,80 1440,45...` fill `#f2f5f9`). Below-header content stays in `mx-auto max-w-7xl space-y-7`. Verified: `tsc --noEmit` EXIT 0; `.next` wiped + `osca_frontend` restart; compiled chunk `src_app_dashboard_page_tsx_5b2001._.js` shows `jsxDEV(WelcomeHeader)` as sibling before `className:"mx-auto max-w-7xl space-y-7"` div, header wrapper has `-mx-5 -mt-5 ... lg:-mx-7 lg:-mt-7` + `rounded-r-[18px]`; `System Online`=0.
- **Dashboard header final polish** (`frontend/src/app/dashboard/page.tsx` `WelcomeHeader`, header JSX/Tailwind ONLY â€” no other dashboard UI/logic touched): Matches user's reference-image spec. Header: `rounded-[18px]` + `shadow-md` + subtle `ring-1 ring-black/5`, `overflow-hidden relative`, ~160px tall (`min-h-[150px] md:min-h-[160px]`). Background: `/public/NAAP.png` via `url('/NAAP.png')`, `cover` + `background-position: center center`. Navy overlay: `bg-gradient-to-r from-[#061a38]/95 via-[#123b68]/75 to-[#123b68]/45` (photo clearly visible, left darker). Logo: `osca-logo.png` (`h-12 w-12 md:h-16 md:w-16 object-contain shrink-0`) sits DIRECTLY on the photo â€” **no white box/bg-white/ring/wrapper** (PNG is RGBA ColorType=6, corners A=0, genuinely transparent). Branding text: "OSCA Management System" `text-[20px] md:text-[30px] font-extrabold text-white`; "Office of Sports and Cultural Affairs" `text-[13px] md:text-[16px] text-white/95`; "NAAP â€“ Villamor Campus" `text-[11px] md:text-[13px] text-blue-100/90`. Right: glass date/time panel `rounded-2xl border border-white/20 bg-white/10 backdrop-blur-md` (Calendar icon + `format(now,"EEEE, MMMM d, yyyy")` + live `hh:mm:ss a`, existing 1s timer unchanged). **No "System Online" / green dot / online badge anywhere** (count 0 in compiled chunk). Bottom wave: absolutely-positioned SVG `h-[38px] md:h-[45px]` viewBox `0 0 1440 100` preserveAspectRatio none, path `M0,65 C240,20 480,85 720,55 C960,25 1200,80 1440,45 L1440,100 L0,100 Z` fill `#f2f5f9` (matches dashboard bg `#f2f5f9` â†’ seamless transition), overlaps photo bottom, doesn't cover logo/clock. Responsive: mobile = smaller logo/text/clock (`self-start` column layout), no overlap. Verified: `tsc --noEmit` EXIT 0; `.next` cache wiped + `osca_frontend` restart (bind-mount watcher doesn't detect edits on Windows/Docker); `/dashboard` â†’ 200; compiled chunk `src_app_dashboard_page_tsx_5b2001._.js` (copied via `docker cp`) contains `rounded-[18px]`, `center center`, `061a38`/`123b68`/`45`, `M0,65 C240,20`, `f2f5f9`, `30px`, `osca-logo.png`+`h-12 w-12 shrink-0 object-contain` (no `bg-white` around it), `rounded-2xl` clock â€” and `System Online`=0, `emerald-400`=0. No backend/API/permission changes.
- **Unified LIGHT dashboard redesign for ALL roles** (`frontend/src/app/dashboard/page.tsx`): Replaced the dark NAAP hero (removed entirely) with ONE shared `WelcomeHeader` used by every role â€” white card, subtle NAAP campus photo at `opacity-[0.07]`, soft light-blue gradient (`from-white via-[#f7faff] to-[#e8f1fc]`), bottom wave decoration (fill `#dbe7f8`, `preserveAspectRatio="none"`), "Welcome back, {first_name}! ðŸ‘‹" + role subtitle via `WELCOME_SUBTITLES` map (admin/staff "Here's what's happening with OSCA today.", director "overview of OSCA operations", coach "team and attendance", pe_instructor "activities and OSCA updates", student "attendance, OSCA announcements, and account information"), and right-side date + live time (`hh:mm:ss a`) panel. Content stays role-specific: `ManagerView` (admin/director/staff: KPIs + weekly/equipment charts + role-aware QuickActions + announcements + pending alert) vs new `RoleView` (coach/pe_instructor/student: role-filtered KpiCard stats + Attendance This Week + Equipment Status [hidden for students] + announcements). `LegacyView` renamed `RoleView`, restyled to identical light cards (`rounded-2xl border border-gray-100 bg-white p-6 shadow-sm`), charts unified (`#1557C0` bars, `h-[220px]`). `KpiCard` icon box now takes full class string (`iconCls` includes `bg-gradient-to-br` for managers, solid `bg-*` for legacy). Removed unused `next/image` import. Verified: `tsc --noEmit` clean; all 6 roles (admin/director/staff/coach/pe_instructor/student) login + `/dashboard` â†’ 200 after `.next` cache clear; compiled chunk `src_app_dashboard_page_tsx_5b2001._.js` contains welcome markers (Welcome back, NAAP.png, all WELCOME_SUBTITLES, EEEE/hh:mm:ss, dbe7f8, f7faff, Quick Actions) and NO `System Online`/`bg-gradient-to-r` (dark hero removed). No backend/API/permission changes.
- **Venue Reservation Requests (complete, time-slot based)**: Coach/PE Instructor submit venue reservations (`POST /facilities/reservations` â€” facility_id, purpose, reservation_date, start/end_time, remarks); Admin/Director/Staff approve/reject (`PATCH .../approve`, `PATCH .../reject` with optional reason). **A venue is NEVER locked whole â€” approve does NOT flip facility status to `reserved`**; only overlapping date+time slots are blocked (`_has_overlap`: `start < db_end and end > db_start`; `_conflicting_reservation` checks pending+approved). Multiple same-day reservations allowed if non-overlapping (e.g. 8â€“10 and 10â€“12 both OK; 9â€“11 â†’ 409). Only `maintenance`/`closed` venues block new requests and approval. New endpoint `GET /facilities/{id}/reservations` returns upcoming (today onward, any status) reservations for a venue to non-students. Permission split: `_EDITOR_ROLES = (ADMIN, DIRECTOR, STAFF)` for manage/approve (uses `AdminOnly` dep), `CoachOrPe` for submit, students 403 on both `/facilities` and `/facilities/reservations`. Audit actions VENUE_REQUEST_SUBMITTED/APPROVED/REJECTED + FACILITY_CREATED/UPDATED/DELETED/STATUS_UPDATED/IMAGE_UPLOADED. Notifications via `services/notification_service.py`: `notify_venue_managers` on submit (Admin/Director/Staff), `notify_users` to requester on approve (success) / reject (error). Celery `release_expired_reservations` (15-min beat, Asia/Manila) is legacy (no longer needed since status never flips to RESERVED). New tables `venue_reservation_requests` + `notifications`; `facilities.image` column + `RESERVED` in `facility_status_enum` (value kept for backward compat/display only). Migration `b1c2d3e4f5a6_add_venue_reservations_notifications.py` applied (idempotent â€” tables pre-created by startup `create_all`). Frontend: `types/index.ts` (VenueReservation, ReservationCreate/Reject, NotificationItem/ListRead, Facility.image_url, status `reserved`), `api.ts` (`facilitiesApi.delete/uploadImage/listReservations/listVenueReservations/createReservation/approveReservation/rejectReservation` + `notificationsApi`), `useNotificationStore.ts` now backend-polling (layout bell polls every 30s, click marks read), Facilities page role-aware (manager: Add Venue/edit/delete/upload image; requester: Request Venue modal; student: Access Denied guard) â€” **Request Venue dropdown disables only maintenance/closed**, View Details modal now shows an Upcoming Reservations table (Date | Time | Purpose | Status) fetched via `GET /facilities/{id}/reservations`, new `dashboard/facilities/reservations/page.tsx` (Requester|Venue|Purpose|Date|Time|Status|Requested At|Actions; status filter; View modal; Approve/Reject + reason modal for managers; students 403 guard), nav child "Venue Reservations" under Facilities for admin/director/staff/coach/pe_instructor. Live-verified: 8â€“10 â†’ 201, 10â€“12 same day â†’ 201, 9â€“11 â†’ 409, approve leaves venue `available`, upcoming endpoint lists all slots; `tsc --noEmit` + `py_compile` pass; `osca_frontend` restarted.
- **Venue Reservation Requests (complete, time-slot based)**: Coach/PE Instructor submit venue reservations (`POST /facilities/reservations` â€” facility_id, purpose, reservation_date, start/end_time, remarks); Admin/Director/Staff approve/reject (`PATCH .../approve`, `PATCH .../reject` with optional reason). **A venue is NEVER locked whole â€” approve does NOT flip facility status to `reserved`**; only overlapping date+time slots are blocked (`_has_overlap`: `start < db_end and end > db_start`; `_conflicting_reservation` checks pending+approved). Multiple same-day reservations allowed if non-overlapping (e.g. 8â€“10 and 10â€“12 both OK; 9â€“11 â†’ 409). Only `maintenance`/`closed` venues block new requests and approval. New endpoint `GET /facilities/{id}/reservations` returns upcoming (today onward, any status) reservations for a venue to non-students. Permission split: `_EDITOR_ROLES = (ADMIN, DIRECTOR, STAFF)` for manage/approve (uses `AdminOnly` dep), `CoachOrPe` for submit, students 403 on both `/facilities` and `/facilities/reservations`. Audit actions VENUE_REQUEST_SUBMITTED/APPROVED/REJECTED + FACILITY_CREATED/UPDATED/DELETED/STATUS_UPDATED/IMAGE_UPLOADED. Notifications via `services/notification_service.py`: `notify_venue_managers` on submit (Admin/Director/Staff), `notify_users` to requester on approve (success) / reject (error). Celery `release_expired_reservations` (15-min beat, Asia/Manila) is legacy (no longer needed since status never flips to RESERVED). New tables `venue_reservation_requests` + `notifications`; `facilities.image` column + `RESERVED` in `facility_status_enum` (value kept for backward compat/display only). Migration `b1c2d3e4f5a6_add_venue_reservations_notifications.py` applied (idempotent â€” tables pre-created by startup `create_all`). Frontend: `types/index.ts` (VenueReservation, ReservationCreate/Reject, NotificationItem/ListRead, Facility.image_url, status `reserved`), `api.ts` (`facilitiesApi.delete/uploadImage/listReservations/listVenueReservations/createReservation/approveReservation/rejectReservation` + `notificationsApi`), `useNotificationStore.ts` now backend-polling (layout bell polls every 30s, click marks read), Facilities page role-aware (manager: Add Venue/edit/delete/upload image; requester: Request Venue modal; student: Access Denied guard) â€” **Request Venue dropdown disables only maintenance/closed**, View Details modal now shows an Upcoming Reservations table (Date | Time | Purpose | Status) fetched via `GET /facilities/{id}/reservations`, new `dashboard/facilities/reservations/page.tsx` (Requester|Venue|Purpose|Date|Time|Status|Requested At|Actions; status filter; View modal; Approve/Reject + reason modal for managers; students 403 guard), nav child "Venue Reservations" under Facilities for admin/director/staff/coach/pe_instructor. Live-verified: 8â€“10 â†’ 201, 10â€“12 same day â†’ 201, 9â€“11 â†’ 409, approve leaves venue `available`, upcoming endpoint lists all slots; `tsc --noEmit` + `py_compile` pass; `osca_frontend` restarted.
- **500 fix on reservation create**: `audit_log()` writes `new_values` into a JSONB column; passing `body.model_dump()` with UUID/date/time crashed (`refresh operation cannot proceed` surfaced as 500). Added `_jsonable()` helper in `facilities.py` (stringifies UUID/date/time/datetime) and applied it to the reservation + schedule audit calls.
- **MinIO presigned URL signature fix** (`storage_service.py` `get_presigned_url`): boto3 signs for internal host `minio:9000`, then the URL host was string-replaced to `localhost:9000` â€” SigV4 includes `host` in signed headers â†’ 403. Now presigns against `MINIO_PUBLIC_ENDPOINT` directly (boto3 computes locally, no network call). Fixes venue images, profile pictures, announcements, and face images. Verified via API: image upload â†’ `image_url` returns 200.
- **Facilities page redesign** (`frontend/src/app/dashboard/facilities/page.tsx`): Module now manages ONLY the six venues (Covered Court, Upper Gym, Band Room, CAU Studio, Open Ground, Weights Room) rendered as modern image cards from `/public/{covered_court,upper_gym,band_room,cau_studio,open_ground,weights_room}.{png,jpg}`. 16:9 cover (`aspect-video` + `object-cover`, rounded top corners via `overflow-hidden`), dark gradient overlay (`from-black/70 via-black/10`), venue name on image, status badge overlay top-right (Available=green, Under Maintenance=yellow, Closed=red; `in_use`â†’blue kept as safety). `VenueImage` component falls back to a gradient + Building2 placeholder on missing image (`onError`). View Details modal (all roles: image header, status, date created, last updated via `formatDateTime`) + Edit modal (admin/director/staff: status dropdown with only available/maintenance/closed, description, notes â†’ `PATCH /facilities/{id}`). Responsive grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`; hover `shadow-lg -translate-y-1`. REMOVED from Details modal: capacity, condition, location, description, notes (obsolete fields). REMOVED: Add Facility button/modal, `VenueSelect`, all Schedule UI (schedules query + modal), Training/GYM records deactivated in DB (`is_active=false`). `seed_all.py` facility list replaced with the six venues. DB seeded via psql (6 facilities active). `tsc --noEmit` + `py_compile` pass; `osca_frontend` restarted; images verified 200.
- **Add Facility modal revamp** (`frontend/src/app/dashboard/facilities/page.tsx`): Removed Capacity field (form state, submit payload, card display). Renamed Location â†’ Venue using a searchable dropdown (`VenueSelect` component: button + search input + filtered option list, closes on outside click) with options Covered Court / Upper Gym / Band Room / CAU Studio / Open Ground / Weights Room (still submitted as backend `location`). Added Lucide icons (Building2 / FileText / MapPin), placeholders ("e.g. Basketball Practice" / "Enter a brief description..." / "Select a venue"), subtitle "Enter the facility details below.", and bottom-right Cancel + Create Facility buttons. Backend `capacity` is optional (`int | None`) so omitting it is safe. `tsc --noEmit` pass; `osca_frontend` restarted.
- **Users table ID column**: `UserSummary` schema (backend `schemas/user.py` + frontend `types/index.ts`) now includes `employee_id`. Table header label is dynamic by `roleFilter`: All Roles â†’ "ID Number", Students â†’ "Student ID", other single-role filters â†’ "Employee ID". Cell shows `student_id` for students, `employee_id` for admin/director/staff/coach/pe_instructor, and "â€”" only when the value is absent. Verified live via `/users` API (employee_id present) + `tsc --noEmit` + `py_compile`.
- **Extended Attendance audit logging**: Added `_attendance_audit()` helper in `attendance.py` that packs the full required field set into `details` (student_name, student_id, sport_or_art, session_name, date_time, attendance_status, method [facial_recognition/qr_code/manual], action, performed_by, result, failure_reason) while reusing the IP/browser/OS capture path. All audit log rows added before a raised `HTTPException` are explicitly `db.commit()`-ed first (since `get_db()` rolls back on exception). New endpoints: `POST /sessions/{id}/open` (SESSION_OPENED), `DELETE /sessions/{id}` (SESSION_DELETED, cascades records), `POST /attendance/manual` (MANUAL_ATTENDANCE_ADDED + MANUAL_TIME_IN/TIME_OUT), `PATCH /attendance/manual/{record_id}` (MANUAL_ATTENDANCE_EDITED + ATTENDANCE_STATUS_CHANGED + MANUAL_TIME_IN/TIME_OUT), `DELETE /attendance/manual/{record_id}` (MANUAL_ATTENDANCE_DELETED), `POST /attendance/qr/check-in` (QR_CODE_VALIDATION_FAILED / INVALID_SESSION_QR / blocked / ATTENDANCE_TIME_IN via `STU-{uuid}` QR). `face_scan` now logs EVERY outcome: attempt-failed (no session/inactive/FR fail), attempt-after-session-closed, attempt-blocked (PE Instructor), attempt-ineligible (sport mismatch), attempt-inactive-account, attempt-blocked-sanction (new ACTIVE-sanction + end_date guard blocks check-in), attempt-outside-allowed-time (TIME_IN after `scheduled_end + grace`), duplicate-attempt (warning, idempotent), time-in/time-out success. `end_session` action renamed `SESSION_ENDED` â†’ `SESSION_CLOSED`. Schemas added: `ManualAttendanceCreate`/`ManualAttendanceUpdate`/`QrCheckInRequest`. Verified live end-to-end: create session â†’ bad QR 422 + QR_CODE_VALIDATION_FAILED â†’ QR check-in success (present) â†’ duplicate warning â†’ manual add (late) â†’ edit (excused + ATTENDANCE_STATUS_CHANGED) â†’ close (SESSION_CLOSED) â†’ delete record + session (MANUAL_ATTENDANCE_DELETED + SESSION_DELETED); full `details` payload confirmed in DB. `py_compile` + `tsc --noEmit` pass. NOTE: QR check-in is backend-only (no frontend scanner yet); facescan remains the only UI check-in flow. NOTE: active-sanction and outside-allowed-time now BLOCK face check-in (new behavior).
- **Audit Logs module completed**: All inline `AuditLog(...)` writes converted to `audit_log()` service calls across auth/users/attendance/inventory/admin; NEW log sites added for logout, equipment update, borrowing ID issued, session create/update/end, attendance time-in/out, announcements, facilities, eligibility, incidents, sanctions (incl. acknowledge), and report generate/export (daily/weekly/monthly only when `format != json`). `_EDITOR_ROLES` narrowed to Admin-only; filter endpoints now enforce it. Frontend: Admin-only guard + date-range filters + new module colors + nav link. Verified live: login success/failed capture IP+browser+OS; logout logs; Director gets 403; CSV export returns full metadata columns; equipment create logs with resource_id. `py_compile` + `tsc --noEmit` pass.
- **Role-specific Requests table**: Admin/Director/Staff table: Requester | Items | Expected Return | Requested At | Approved By | Status | View Details | Actions. Actions column (last column) restored for approvers with original functionality: Approve/Reject for pending non-expired, Delete for non-pending, "â€”" otherwise. Return QR/"Show QR" stays requester-only (Coach/PE Instructor). Approve/Reject/Delete also available in View Details modal footer (approver-only). No Show QR/Return QR in the Actions column.
- **Delete Equipment Request**: Backend `DELETE /requests/{id}` (Admin/Director/Staff) â€” deletes PENDING/REJECTED/CANCELLED and approved requests whose linked transaction is RETURNED; blocks with 409 when the linked borrow is still active/overdue/partial. Frontend `inventoryApi.deleteRequest` + `DeleteConfirmModal` + red "Delete" button in Actions column for non-pending rows (restored Actions column with Cancel/Approve/Reject). Requests page table now: Requester | Items | Expected Return | Requested At | Approved By | Status | View Details | Return QR | Actions; removed top "Scan QR" button; button label "Show QR"; table wrapped in `overflow-x-auto` for responsive scroll.
- **Request-tied Return QR**: `return_qr_code` now generated at REQUEST CREATION (`TXN-{request_id.hex[:12].upper()}`), stored on `EquipmentRequest` (new column, migration `9a8b7c6d5e4f`), and REUSED on the BorrowTransaction at approve/release â€” so the QR the requester sees is the exact same code that gets scanned and invalidated. Fixed `_build_request_read`: it no longer attaches the requester's "latest transaction" QR (which could be a wrong/already-used QR from a different request) â€” it reads `req.return_qr_code` directly and looks up status by that code. Legacy approved requests without `return_qr_code` keep the old fallback. Verified via API: create â†’ `TXN-F5C95A02A50E`, approve â†’ active, scan â†’ borrower/items, complete â†’ returned + invalidated, request shows `return_qr_status: used`.
- **Student Dashboard Welcome**: Personalized "Welcome, {First Name}!" heading for students with descriptive subtitle; non-students see original "Dashboard" heading
- **Student Attendance Trend**: Weekly bar chart now uses real attendance records (last 7 days) instead of random mock data
- **Student Attendance stat**: "Attendance Today" card shows the student's own scan count instead of global total
- **My Attendance page**: Added search input + Daily/Weekly/Monthly filter buttons; columns changed to Attendance Date, Sport/Art, Attendance Session, Status, Time In, Time Out; paginated with 20 records/page
- **Backend GET /records**: Added `date_from`, `date_to` query params for date-range filtering; joined with `Session` to populate `session_name` and `session_sport_or_art` in response
- **Schema**: `AttendanceRecordRead` now includes `session_name` (str) and `session_sport_or_art` (str | None)

### Completed (current session)
- **Dashboard header redesign (UI only, header only)**: `frontend/src/app/dashboard/page.tsx` `WelcomeHeader` â€” WIDE navy-overlaid NAAP banner: `background-image: url('/NAAP.png')` + `background-size: cover` + `background-position: center 45%`; dark navy transparent overlay `bg-gradient-to-r from-[#061a38]/90 via-[#123b68]/70 to-[#0b2b50]/60` (building stays visible); `rounded-[20px]` + `overflow-hidden` + `shadow-md`; ~150px (mobile) / 165px (md+) tall via `min-h-[150px] md:min-h-[165px]`; horizontal padding `px-7 md:px-10`; LEFT = white logo tile `h-[60px] md:h-[64px]` + "OSCA Management System" (large extrabold white) / "Office of Sports and Cultural Affairs" (medium blue-100) / "NAAP â€“ Villamor Campus" (smaller blue-200/80); RIGHT = single glass date+live-clock panel (`EEEE, MMMM d, yyyy` + `hh:mm:ss a`, existing 1s timer kept); subtle bottom wave (`fill:#f2f5f9`, `pointer-events-none`, `h-[26px] md:h-[34px]`, blends into light bg); "System Online" pill REMOVED completely. NO other dashboard UI/functionality touched (WelcomeSection, cards, charts, Quick Actions, announcements all unchanged). Verified: `tsc --noEmit` EXIT 0; dev server fresh-compiled `/dashboard` 200 (stale `.next` volume wiped again â€” `rm -rf /app/.next/*` + restart; bind-mount watcher does not detect edits on this Windows/Docker setup, authenticated request required to compile the page); chunk `src_app_dashboard_page_tsx_5b2001._.js` contains `center 45%`/`061a38`/`123b68`/`0b2b50`/`bg-gradient-to-r`/`rounded-[20px]`/`min-h-[150px]`/`md:px-10`/`f2f5f9`/`M0,44` AND `System Online`=0/`emerald-400/15`=0/`linear-gradient(105deg`=0; `Welcome back`/`ann.id` still present; nginx authenticated `/dashboard` 200. NOTE: kiosk page (`/kiosk`) still has its own "System Online" status text â€” separate component, intentionally untouched.
- **All-role dashboard redesign (UI only, role features/logic preserved)**: `frontend/src/app/dashboard/page.tsx` â€” (1) `WelcomeHeader` is now a WIDE navy-overlaid NAAP banner: `background-image: url('/NAAP.png')` + `cover`/`center`, soft navy gradient overlay (`linear-gradient(105deg, rgba(7,20,41,0.94)â€¦rgba(21,87,192,0.24))`, building stays recognizable), subtle bottom wave (`fill:#f2f5f9`, `preserveAspectRatio="none"`), LEFT = white logo tile + "OSCA Management System" / "Office of Sports and Cultural Affairs" / "NAAP â€“ Villamor Campus" (white text), RIGHT = glass date+live-clock panel (`EEEE, MMMM d, yyyy` + `hh:mm:ss a`, existing 1s timer kept) + "â— System Online" pill (ping dot). (2) New `WelcomeSection` below banner: "Welcome back, {first_name}! ðŸ‘‹" + role-specific subtitle from new `WELCOME_SUBTITLES` map (admin/staff "Here's what's happening with OSCA today.", director "â€¦overview of OSCA operations today.", coach "â€¦your team, sessions, and today's attendance.", pe_instructor "â€¦OSCA activities today.", student "â€¦your attendance, updates, and account overview."). (3) Spacing: dashboard content wrapped in `mx-auto max-w-7xl space-y-7`; ManagerView/RoleView section spacing `space-y-6`â†’`space-y-7`; KPI grid `gap-5`; card corner radius unified `rounded-2xl`â†’`rounded-xl` on dashboard cards; Updates & Notices header now has subtitle "Latest announcements and OSCA updates". (4) `layout.tsx`: light page bg `#f5f6f8`â†’`#f2f5f9` (subtle gray-blue) on shell + spinner + main; main content padding `p-4 lg:p-6`â†’`p-5 lg:p-7`; globals.css added `.dark .bg-[#f2f5f9]` override. NO changes to auth, RBAC, API calls, React Query, QUICK_ACTIONS roles, charts data, announcements/ack/comment logic, sidebar nav, or dark-mode. Verified: `tsc --noEmit` EXIT 0; dev server cold-compiled `/dashboard` 200 after clearing stale `.next` volume (`rm -rf /app/.next/*` + restart â€” bind-mount watcher had stopped detecting edits, a known Windows/Docker Desktop issue); new chunk `src_app_dashboard_page_tsx_5b2001._.js` contains `System Online`/`Welcome back`/`NAAP.png`/`linear-gradient(105deg`/`f2f5f9`/`ann.id`/`unacknowledge`/`acknowledgement_count`/`Latest announcements and OSCA updates`/all QUICK_ACTIONS labels; `bg-[#f2f5f9]` compiled in CSS; nginx `/dashboard` (auth) 200 + API `/health` healthy. NOTE: verified via chunk markers (client components render post-hydration; SSR HTML only contains the shell).
- **Announcement Acknowledge + Comments (complete, live-verified)**: Feature was missing on BOTH backend and frontend (static buttons did nothing). Backend `announcements.py` (still no `Request` usage): `list_announcements` rewritten (bulk creator names, ack counts, comment counts, `my_acked`, order pinned desc/event_date asc nullslast/created_at desc, returns `pages`); new endpoints `POST /{id}/acknowledge` (201, idempotent via unique `uq_announcement_ack_user`), `DELETE /{id}/acknowledge` (204, no-op if none), `GET /{id}/comments` (paginated, created_at asc), `POST /{id}/comments` (201, strips + validates content; whitespace-only rejected). New models `AnnouncementAcknowledgement` + `AnnouncementComment` (`models/announcement.py`, FKs `ON DELETE CASCADE`, registered in `models/__init__.py`); schemas: `AnnouncementRead` + `acknowledged_by_me`/`acknowledgement_count`/`comment_count`, new `AcknowledgementRead`, `CommentCreate` (1â€“1000), `CommentRead` (incl. `author_name`). Migration `c3d4e5f6a7b8_add_announcement_acknowledgements_comments.py` (down `b1c2d3e4f5a6`, idempotent, applied). Audit actions: ANNOUNCEMENT_ACKNOWLEDGED / ACKNOWLEDGEMENT_REMOVED / COMMENTED. Frontend: `api.ts` (`acknowledge`/`unacknowledge`/`comments`/`addComment`), `types/index.ts` (`Announcement` optional fields + `AnnouncementComment`), `dashboard/page.tsx` `AnnouncementsFeed` â†’ container + `AnnouncementCard` (ack toggle w/ spinner+count badge, comment textarea + Post w/ loading/error banner, comments `useQuery` key `["announcements", ann.id, "comments"]`, invalidations). **ROOT-CAUSE FIX for GET 500s**: `select(User.id, User.full_name)` failed because `full_name` is a Python `@property`, not a column ("Column expression... got <property object>") â€” now `select(User).where(...)` + `u.full_name` dict comprehension in both list + comments. Live-verified: admin/coach/student all ack (idempotent same `created_at` on repeat; toggle-off â†’ count 0 â†’ re-ack new timestamp) + comment (author names correct); per-user `acknowledged_by_me` correct; counts aggregate across users; bad ID â†’ 404; audit rows present; test data + audit rows cleaned up. `py_compile` + `tsc --noEmit` pass; both containers restarted; compiled dashboard chunk `src_app_dashboard_page_tsx_5b2001._.js` contains `unacknowledge`/`acknowledgement_count`/`ann.id` markers. NOTE: early `read`/`grep` outputs in this session were corrupted/hallucinated (phantom 195-line file with `request: Request`; phantom already-wired UI) â€” ground truth required PowerShell `Select-String`/accurate chunked reads.
- **Reports module redesign (complete, live-verified post-restart)**: `/dashboard/reports` is now a tab-based, card-driven page (Attendance, Inventory, Facilities, Eligibility, Incidents, Sanctions) with per-card generate + PDF/Excel/CSV export, backed by 19 new format-parameterized endpoints in `backend/app/api/v1/reports.py` (`?format=json|csv|xlsx|pdf`): `/attendance/{daily,weekly,monthly}`, `/inventory/{equipment,borrowing-history,returned,lost-damaged}`, `/facilities/{venue-reservations,venue-usage,status}`, `/eligibility/{eligible,restricted,ineligible}`, `/incidents/{reports,categories,summary}`, `/sanctions/{active,completed,history}`. RBAC: inventory endpoints `AdminOnly` (admin/director/staff, coach 403); others `NotStudent` (students 403). Bugs fixed in this session: `_build_monthly_csv` 3-tuple unpack, tz-aware datetimes crashing openpyxl (`_xlsx_val()` â†’ naive UTC), wide tables crashing xhtml2pdf (landscape + `table-layout: fixed`), `Lost/Damaged` sheet `/` sanitization. Full matrix 76/76 combos â†’ 200 post-restart; RBAC spot-checks pass (coach 403 inventory / 200 others, staff 200 all, student 403 all; temp zz_* accounts permanently deleted); Reports nav now includes staff (`layout.tsx`). Commits `929652f` + `0fdf36c` + `d8c5338`. NOTE: first `docker compose up` after a Docker Desktop restart can 502/504 for minutes while osca_api re-downloads the insightface `buffalo_l` model and osca_frontend recompiles (~3â€“4 min); healthcheck path is `/health` (container-internal), not `/api/v1/health`.

### Completed (current session)
- **Coach Attendance Scan access (complete, sport-scoped)**: Coaches now operate the Attendance/kiosk flow for their own `assigned_sport` only. Backend `attendance.py`: helpers `_validate_coach_session_sport` (create) + `_enforce_coach_session_scope` (audit-logged 403 on other-sport sessions) applied to get/update/open/end/delete session, manual attendance add/edit/delete, `face_scan`, `qr_check_in`; `get_attendance_records` filtered to coach's sport; `update_session` audit uses new `_jsonable()`; `enroll_face` restricted to STUDENTs of coach's `assigned_sport` (403 otherwise). Fixed latent `_attendance_audit` crash (`scan_type.value` on a plain string â†’ `getattr(scan_type, "value", scan_type)`). Frontend (`attendance/page.tsx`, `roster/page.tsx`): `userSport = isCoach ? assigned_sport : sport_or_art`; NewSessionModal + EditSessionModal lock Sport/Art (read-only) for coaches, payload keeps own sport; roster `sport` uses `assigned_sport`. Nav: Attendance Scan `/kiosk` roles `["admin", "coach"]`; students keep the self-scan page but no kiosk. Live-verified: coach 403 on Volleyball sessions (create/get/update/end/open/delete/manual/qr/face), 200 on own Basketball sessions, records scoped to own sport; enrollment 403 on out-of-sport student, passes on in-sport (FR decode 422 on dummy image only). `py_compile` + `tsc --noEmit` pass; `osca_frontend` restarted.
- **Fixed 500 on `/users/register`** (`users.py:106`): `UserCreate` uses `use_enum_values=True`, so `user.role` is a plain string pre-flush â†’ `user.role.value` in the USER_REGISTERED audit description crashed with `AttributeError`, and the Coach/PE BorrowingID auto-generation branch silently skipped. Added `if not isinstance(user.role, UserRole): user.role = UserRole(user.role)` right after `User(...)` construction. Verified live: admin creates student â†’ 201 (role returned as enum); temp test student created + permanently deleted.
- **Missing migration applied**: DB was at `e7f8a9b0c1d2`, head is `f3g4h5i6j7k8`. Ran `alembic upgrade head` in `osca_api` container â€” `request_status_enum` now has `cancelled`. This was causing 500 errors (`invalid input value for enum request_status_enum: "CANCELLED"`) on `GET /requests?status=cancelled`.
- **Backend return-QR chain verified end-to-end** via API: create request â†’ approve `{create_transaction: true}` â†’ `return_qr_code` populated (`TXN-{id_hex[:12].upper()}`) â†’ `scan_transaction_qr` returns borrower/items/status â†’ `complete_transaction` marks returned + invalidates QR. Works.
- **Manual TXN code entry added** to Return Scanner (`return-scanner/page.tsx`): staff can type/paste the `TXN-` code (bypasses camera) â€” also a diagnostic for camera/decode issues. `processCode()` shared by scan callback + manual form.
- **Naive datetime hardening**: `schemas/inventory.py` validators (`return_must_be_future` in `BorrowTransactionCreate`, `EquipmentRequestCreate`, `StaffBorrowCreateRequest`) now treat naive `expected_return` as UTC instead of crashing with `TypeError` â†’ 500.
- **Requests page (admin/director/staff)**: (1) Date range filter â€” backend `GET /requests` accepts `date_from`/`date_to` (filter on `requested_at`), frontend adds two `<input type="date">` (sends `T00:00:00` / `T23:59:59`); (2) removed the read-only "Approved" badge from the Actions column; (3) derived "Returned" status badge shown when a request is `approved` + `return_qr_status === "used"` (i.e. the linked transaction was completed) â€” added `returned` entry to `statusConfig` + `RotateCcw` icon; "Show Return QR" button now hidden once `return_qr_status === "used"`.
- **DB enum case bug fixed**: SQLAlchemy binds enum member NAMES (uppercase, e.g. `CANCELLED`), but migration `f3g4h5i6j7k8` added lowercase `'cancelled'` â†’ `GET /requests?status=cancelled` still 500. Added uppercase `'CANCELLED'` to `request_status_enum` directly in DB + updated the migration file. (Pre-existing enums are uppercase, e.g. `PENDING`/`APPROVED`/`REJECTED`.)
- **Camera QR scan hardened (open item resolved)**: Added shared hook `frontend/src/hooks/useQrScanner.ts` and refactored BOTH scanner pages (borrow-scanner + return-scanner) to use it. It opens the camera with `facingMode: "environment"` first, then FALLS BACK to `decodeFromConstraints({ video: true })` (any camera) when the first attempt rejects â€” this fixes laptops/desktops whose only webcam can't satisfy `facingMode: "environment"` (the "nothing happens / decode never fires" symptom). Video element now also gets `autoPlay muted playsInline`. A fresh `docker restart osca_frontend` resolves the other suspected cause (stale bundle â€” first compile on the slow container filesystem took ~140s). Manual TXN entry remains as the camera-less fallback. `tsc --noEmit` pass; both `/dashboard/inventory/borrow-scanner` and `/return-scanner` serve 200.
- **Return QR root cause fixed**: `approveRequest` in `requests/page.tsx` was sending `{ create_transaction: false }`, so approving a request created NO BorrowTransaction and NO dynamic Return QR (`return_qr_code` stayed null â†’ "Show Return QR" button never appeared). Changed to `create_transaction: true` so approval immediately creates the transaction + `TXN-{id_hex[:12].upper()}` Return QR. Removed the obsolete 60-minute request-QR expiry check in `approve_equipment_request` (requests no longer need a QR). Removed duplicate `ReturnQRModal` render in `requests/page.tsx`. `tsc --noEmit` + `py_compile` pass.
- **Return QR root cause fixed**: `approveRequest` in `requests/page.tsx` was sending `{ create_transaction: false }`, so approving a request created NO BorrowTransaction and NO dynamic Return QR (`return_qr_code` stayed null â†’ "Show Return QR" button never appeared). Changed to `create_transaction: true` so approval immediately creates the transaction + `TXN-{id_hex[:12].upper()}` Return QR. Removed the obsolete 60-minute request-QR expiry check in `approve_equipment_request` (requests no longer need a QR). Removed duplicate `ReturnQRModal` render in `requests/page.tsx`. `tsc --noEmit` + `py_compile` pass.
- **Nav links restored**: `layout.tsx` Inventory menu now includes Borrow Scanner + Return Scanner (Admin/Director/Staff only).
- **api.ts + pre-existing compile errors fixed**: Added `staffBorrowApi`, `inventoryApi.cancelRequest`, `reportsApi.dailyAttendance/weeklyAttendance/monthlyAttendance`; imported `User` type in users page; fixed `TagIcon` JSX in dashboard page. `tsc --noEmit` passes with zero errors.

### Completed (previous sessions)
- **Auto-generate Digital ID**: BorrowingID auto-created for Coach/PE Instructor on account creation (`users.py:116-133`). Also auto-generated on profile fetch (`GET /borrowing-ids/me`) if missing. Removed "Contact OSCA administrator" message from profile page.
- **Cancel Request**: Added `CANCELLED` to `RequestStatus` enum, backend cancel endpoint (`PUT /requests/{id}/cancel`), frontend `CancelConfirmationModal` + Cancel button for own PENDING requests. Migration `f3g4h5i6j7k8`.
- **Borrow Scanner page**: Two-step flow at `/dashboard/inventory/borrow-scanner` â€” scan Static QR â†’ identity info, then scan Transaction QR â†’ release/complete actions. Visible to Admin/Director/Staff.
- **Coach nav alignment**: NOT IMPLEMENTED (deferred by user). Coach still has Reports, Analytics, Facilities, Eligibility, Incidents, Sanctions + Attendance/Roster/Kiosk.
- **Reports export columns**: Daily/Weekly/Monthly attendance exports include Student Role and Attendance Date
- **Attendance record eager loading**: Added `selectinload(AttendanceRecord.student)` to eliminate N+1 queries
- **Facility quantity validation**: Backend `ge=0`, frontend `min="0"` with explicit error message
- **Five earlier module updates**: Inventory QR removal, static QR borrowing, attendance reports, facility capacity validation, delete-user cascade fix, account creation improvements
- **Attendance session filtering**: Backend filters sessions by student's `sport_or_art`; PE Instructor rejected with 403
- **Create User form enhancements**: suffix, address, date_of_birth, gender, employee_id, department fields; migration `e7f8a9b0c1d2`
- **Login failure fix**: Migration `e7f8a9b0c1d2` applied â€” existing users table has 6 new columns

## Relevant Files
- `backend/app/models/user.py`: `User` — account status `is_active` (self-registration creates PENDING account, `is_active=False`); NO `email_verified` column (email OTP feature removed/rolled back 2026-08-11)
- `frontend/src/app/(auth)/register/success/page.tsx`: restored message "Your account has been created and is now **pending approval**." + "You will be able to sign in once an administrator activates your account."
- `backend/app/api/v1/auth.py`: login/refresh/logout/password change only — `/auth/registration/send-otp` + `/verify-otp` REMOVED (both 404)
- `backend/app/api/v1/users.py`: `create_user` self-registration → student role forced, `is_active=False` (PENDING approval), no OTP gate; `if not isinstance(user.role, UserRole)` fix so `user.role.value` in audit + Coach/PE BorrowingID branch work
- `backend/app/schemas/auth.py`: `LoginRequest`/`RefreshRequest`/`PasswordChangeRequest`/`TokenResponse` only (OTP schemas removed)
- `frontend/src/app/(auth)/register/page.tsx`: student ID strict format `^\d{5}[A-Z]{2}-\d{6}$` + auto-uppercase (kept); NO OTP modal/state/handlers (removed)
- `frontend/src/lib/api.ts`: `authApi` has no registration OTP calls
- `backend/app/api/v1/facilities.py`: rewritten for venue reservations + venue management (RBAC: `_EDITOR_ROLES` admin/director/staff via `AdminOnly`, `CoachOrPe` submit, students 403); `_jsonable()` helper for audit `new_values`; reservation list/create/approve/reject; `GET /facilities/{id}/reservations` (upcoming slots, non-students); image upload; legacy schedules kept
- `backend/app/models/reservation.py` / `notification.py` / `facility.py`: `VenueReservationRequest` (+`ReservationStatus`), `Notification`, `Facility.image` + `FacilityStatus.RESERVED` + `venue_reservations` rel
- `backend/app/schemas/facility.py` (+ `schemas/notification.py`): `FacilityRead.image_url`, `ReservationCreate/Read/Reject`, `NotificationItem/ListRead`
- `backend/app/api/v1/notifications.py` + `router.py`: `GET /notifications`, `PATCH /{id}/read`, `PATCH /read-all`
- `backend/app/services/notification_service.py`: `notify_users` + `notify_venue_managers` (Admin/Director/Staff)
- `backend/app/services/storage_service.py`: `get_presigned_url` now presigns against `MINIO_PUBLIC_ENDPOINT` (SigV4 host fix); `resolve_venue_image_url`
- `backend/app/workers/tasks.py` / `celery_app.py`: `release_expired_reservations` (15-min beat, Asia/Manila)
- `backend/alembic/versions/b1c2d3e4f5a6_add_venue_reservations_notifications.py`: applied idempotent migration (head now `b1c2d3e4f5a6`)
- `frontend/src/app/dashboard/facilities/page.tsx`: role-aware 6-venue cards + Add Venue / Edit + image upload / Delete / Request Venue modals; View Details modal shows Upcoming Reservations table; student Access Denied guard
- `frontend/src/app/dashboard/facilities/reservations/page.tsx`: Venue Reservation Requests management page (managers all / requesters own; status filter; View modal; Approve/Reject + reason modal)
- `frontend/src/store/useNotificationStore.ts`: backend-polling notification store; `layout.tsx` bell polls every 30s + mark-on-click
- `frontend/src/types/index.ts`: `VenueReservation`, `ReservationCreate/Reject`, `NotificationItem/ListRead`, `Facility.image_url`, `FacilityStatus` includes `reserved`
- `frontend/src/lib/api.ts`: `facilitiesApi.delete/uploadImage/listReservations/createReservation/approveReservation/rejectReservation` + `notificationsApi`
- `backend/app/api/v1/attendance.py`: `_attendance_audit()` helper (packs student/session/status/method/performer/result/failure fields into `details`); session endpoints (create/update/end=SESSION_CLOSED/open/delete); `face_scan()` logs EVERY branch (blocked/ineligible/inactive/sanction/outside-time/duplicate/FR-fail/success) + stores `session_id` on `ScanAttempt`; **`GET /sessions/{id}/latest-attendance`** (kiosk "Last successful attendance", `LatestAttendanceRead` â€” latest successful session scan, enriched with AttendanceRecord for students, identity-only for non-students, falls back to latest record, `has_record: false` when empty); manual attendance endpoints (`POST/PATCH/DELETE /attendance/manual[/{id}]`); QR check-in (`POST /attendance/qr/check-in`, `STU-{uuid}`); `GET /records` (~line 1020) date-range filters + Session join + student auto-filter; **coach-scope helpers** `_validate_coach_session_sport` / `_enforce_coach_session_scope` / `_jsonable` applied to session CRUD + manual + face_scan + qr_check_in + records (sport = `assigned_sport`); `enroll_face` restricted to coach's own-sport students; `_attendance_audit` uses `getattr(scan_type, "value", scan_type)`
- `frontend/src/app/dashboard/attendance/page.tsx`: `userSport = isCoach ? assigned_sport : sport_or_art`; NewSessionModal/EditSessionModal lock Sport/Art (read-only) for coaches and keep own sport in payload
- `frontend/src/app/dashboard/attendance/roster/page.tsx`: roster `sport` uses `assigned_sport` for coaches
- `frontend/src/app/kiosk/page.tsx`: "Last successful attendance" card now DB-backed via `useQuery(["kiosk-latest-attendance", selectedSessionId])` (was local `lastSuccess` state); `attendanceApi.getSessionLatest`; on scan success invalidates `kiosk-latest-attendance` + `kiosk-session-stats`; `LatestAttendance` type in `types/index.ts`
- `backend/app/schemas/attendance.py`: `ManualAttendanceCreate`/`ManualAttendanceUpdate`/`QrCheckInRequest`; `AttendanceRecordRead` includes `session_name`, `session_sport_or_art`
- `backend/app/models/sanction.py`: `SanctionStatus.ACTIVE` + `end_date` guard reused by face_scan + QR check-in to BLOCK attendance under an active sanction
- `frontend/src/types/index.ts` (line 142): `AttendanceRecord` â€” added `session_name`, `session_sport_or_art`
- `frontend/src/app/dashboard/page.tsx`: student welcome heading (lines ~389-402), real attendance trend (lines ~377-400), student today count
- `frontend/src/app/dashboard/attendance/page.tsx`: My Attendance tab â€” search, Daily/Weekly/Monthly filter, updated columns with pagination
- `backend/app/api/v1/users.py` (lines 116â€“133): auto-generate BorrowingID after Coach/PE Instructor creation
- `backend/app/api/v1/inventory.py` (lines 187â€“213): `GET /borrowing-ids/me` â€” auto-generates if missing for Coach/PE Instructor
- `backend/app/api/v1/inventory.py` (lines 541â€“574): `PUT /requests/{id}/cancel` â€” cancel own PENDING request
- `backend/app/api/v1/inventory.py`: `DELETE /requests/{id}` â€” delete request (Admin/Director/Staff); blocks approved requests whose linked borrow is still active/overdue/partial
- `frontend/src/app/dashboard/inventory/requests/page.tsx`: `DeleteConfirmModal` + Delete button (non-pending, approver only); table columns Requester | Items | Expected Return | Requested At | Approved By | Status | View Details | Return QR | Actions
- `frontend/src/app/dashboard/profile/page.tsx`: profile page â€” shows loading spinner while Digital ID is auto-generated
- `frontend/src/hooks/useQrScanner.ts`: shared QR camera hook â€” `facingMode: "environment"` first, falls back to `{ video: true }` (any camera), `autoPlay muted playsInline` on the video element; used by both borrow-scanner and return-scanner pages
- `frontend/src/app/dashboard/inventory/borrow-scanner/page.tsx`: borrow scanner page with two-step QR flow (Static QR â†’ Transaction QR). Static Digital ID workflow unchanged.
- `frontend/src/app/dashboard/inventory/return-scanner/page.tsx`: return scanner page â€” scans dynamic Return QR (TXN-), delegates validation to backend, shows borrower/items/dates/status + OVERDUE badge, allows late returns.
- `frontend/src/app/dashboard/inventory/requests/page.tsx`: `ReturnQRModal` (client-side QR from `return_qr_code`) + `CancelConfirmModal` + Cancel button for own PENDING requests
- `backend/app/api/v1/inventory.py` (line 1150): `complete_transaction` â€” allows late/overdue returns, records `[Late return]` note + `returned_late` audit flag, invalidates QR on completion
- `backend/app/api/v1/inventory.py` (line 414): `get_request_qr_code` â€” legacy `REQ-{id}` endpoint, no longer used for Return QR display
- `frontend/src/lib/api.ts`: `staffBorrowApi` (scanBorrowingId, createBorrow, scanTransactionQr, confirmRelease, completeTransaction) + `inventoryApi.cancelRequest`
- `backend/app/models/inventory.py` (`EquipmentRequest`): `return_qr_code` column â€” generated at request creation, reused on the transaction at approve/release
- `backend/alembic/versions/9a8b7c6d5e4f_add_return_qr_code_to_equipment_requests.py`: adds `return_qr_code` to `equipment_requests` (head now `9a8b7c6d5e4f`)
- `backend/alembic/versions/1f2e3d4c5b6a_add_scan_attempt_session_id.py`: adds nullable `scan_attempts.session_id` FK â†’ `sessions.id` `ON DELETE CASCADE` + index (head now `1f2e3d4c5b6a`, down `d7e8f9a0b1c2`)
- `backend/alembic/versions/3e4f5a6b7c8d_add_announcement_visibility_link.py`: adds `announcements.visibility` (String(20), default+server_default `all_dashboards`) + `announcements.link_url` (String(500) nullable), idempotent `ADD COLUMN IF NOT EXISTS` (current head `3e4f5a6b7c8d`, down `2a3b4c5d6e7f`)
- `frontend/src/app/welcome/WelcomeClient.tsx`: fetches `GET /announcements/public` via React Query (key `["announcements","public"]`, staleTime 60s), `dbNews`/`newsItems`/`carouselItems` (fallback `NEWS_ITEMS`/`ANNOUNCEMENTS`), `NewsCarousel` STRICTLY HORIZONTAL center-focused cover-flow carousel in `#news` (3 cards in ONE row: center focused/large/no-blur z3 and **~28% wider** `centerW = round(cardW*1.28)`; sides scale 0.9/blur(3.5px)/faded 0.55 — RECOGNIZABLE, side offset `round(centerW/2 + cardW*0.45 - 22)` + wide container `min(2*(offset+cardW*0.45)+12, max(600,winW-40))` (954px desktop) so sides fit fully inside (zero clipping), center overlaps sides' inner edge ~22px; NO arrows — side cards CLICKABLE (`onClick={() => setActive(i)}` + cursor pointer + inner `pointerEvents:none` wrapper so a side click moves it to center); auto-advance 6000ms loop, gold dot indicators below (clickable); desktop cardW 320 / tablet 280 / mobile cardW `min(360,max(280,winW*0.86))` + sides hidden) + `CarouselCard` (image→`PublicImageViewer` lightbox, `+N more` badge, `View Link` + whole body `<a target="_blank">` when `link_url`), image click → `PublicImageViewer` portal lightbox (prev/next, counter `i / n`, Esc/arrows, click-outside)
